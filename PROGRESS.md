# Retrospeq build ledger

The one file an agent reads cold to know where the build is. **Hard cap: 200 lines** (`scripts/ledger-check.mjs`, runs in the pre-commit hook). History lives in `docs/ledger/` (append-only, grep it, never read it in full). Standing infra gaps live in `docs/infra-gaps.md`. Things blocking work *now* live in `NEEDS_YOUR_INPUT.md`.

## Autonomy policy

Owner-approved 2026-08-19: agents may commit and push to `main` with no human review gate. This does not waive the spec's own quality bar (AGENTS.md "Security bar" / "Testing bar") — a module is not "done" until those pass. Cost/cadence is the owner's call: local `/loop` only, the cloud routine (`trig_01NV6fHZShY1bPQindEH7dc2`) stays paused, never auto-resume, no "usage exhaustion detection" (owner decision 2026-08-20).

## Phase status

| Phase | Scope | Status (2026-09-14) |
|---|---|---|
| 0 | Golden fixtures + shadow harness | **Done.** 8/8 fixtures (`fixtures/golden/`), harness infra + `shadow_runs`. No real shadow analytics registered yet. |
| 1 | Module 01 Identity · Module 02 Ingestion | **Done 2026-08-23.** |
| 2 | Module 04 Rulebook · Module 08 Onboarding | **04: done for reachable scope** (slices 1–10f). Left: 10c discovery (needs 05 wiring), strategy-scoped rules 1.5–1.7 (blocked on the operand-catalogue product decision in `NEEDS_YOUR_INPUT.md`). **08: partial** — 08a schema, 08b onboarding router + honest-fallback Hook, degraded dashboard (Clear / Trades-to-close + minimal open-position). Left: calibration screen (three seeded rules, §5), silent default strategy (§5.4), full four-state home (position-open card, review-ready), real T0 hook. |
| 3 | Module 03 Fields & Strategy · Module 05 Analytics | **03: done** (backend closed 2026-09-09, all six §5.1 UI elements). Known gap: `strategy_var` owner-scoping (`docs/infra-gaps.md`). **05: backend done** — edge engine, detection engine + v1 catalogue, `rule_proposable`, §4.6 improvement, decay, asset-class suppression, weekday canary. No UI surface of its own (findings render via 03/06). |
| 4 | Module 06 Review · Module 07 Engagement | **06: feature-complete, all gated PASS** (schema, close-out completion, read payload, candidates, ranking/cap, Part 1 UI `/review`, Part 2 graduation + relaxation, promotion/retirement/detection decisions, Part 3 close, deferral backlog + §4.8 expiry, monthly trend, §4.7 adherence-timeline annotation). Left: scheduler (infra gap), phase-end E2E specs. **07: slice 2 done, coder-done** — `engagement_events`/`milestones` ledger, XP accrual (`day_closed`/`review_completed`/`pre_entry_verified`), milestones, frame 1.18 Home line; needs security ‖ qa. Left: the one weekly notification. |
| UI | App shell + visual build | **Shell done (interim, 2026-09-13)**: four tabs, phone-width column, `/settings`, `/performance` placeholder. tester + security PASS, qa stopped by owner. **Next: owner-directed design-system build (owner supplies the process), then remaining features, then screens against the mockup.** `brand/` is visual authority over `modules/09-design-system.md`. |
| v1.1 | Module 09 Prop firms · Module 10 AI | Deferred. |

Cross-cutting follow-ups (details in `docs/infra-gaps.md`): repo-wide Zod `.strict()` sweep · RLS FK-ownership sweep · shared dev project's stale-trade backlog slows `autoConfirmStaleTrades` live tests.

## Current task

**2026-09-16 — UI phase batch 1b (`/accounts/connect` + `/trades/manual-entry` restyle, rows 1.2–1.6) coder-done, needs tester.** 1.2/1.4/1.5/1.6 restyled to their frames and screenshot-verified light+dark, now ●; 1.3 stays ◐, honestly (documented reconciliation, no fake multi-step checklist). Real `.push` stretch-root bug fixed for these two screens the same way batch 1a's qa found for `.dash`/`.hook` (`.connect`/`.entry` added to `components.css`'s `flex:1 1 auto` rule, resynced, proven by screenshot). Found and fixed a real cross-state bug: a stale error from a previous submission stayed on screen under a NEW pending submission's "Checking your account" heading — now gated on `!pending`. `npm run verify` tier 1 PASS. See decision log. Dev server off, all 9 throwaway users deleted.

Batch 1a (Home + onboarding restyle, rows 1.14/1.17/1.9/1.10/1.11/3.6/1.19/1.8) still coder-done, needs tester — unchanged since its own entry below.

Earlier 2026-09-16 (all closed, security PASS ‖ qa PASS unless noted): Module 04 cross-trade operands flipped to `computableToday: true` for real, detection decisions partially unlocked; privacy export completeness; default strategy seed; field-introduction offer; weekly notification. **Owner blocker** (`NEEDS_YOUR_INPUT.md`): only creating the Vercel project. Session vocabulary and custom-field operands were decided 2026-09-15. Phase-end follow-ups unchanged (E2E specs, decisions double-submit ordering, `--block` buttons, `sync.live.test.ts` timeouts, ADR for no-retry send). **Confirmed pre-existing, unrelated**: `decisions-relaxation-integration.live.test.ts`'s CROSS-KIND test fails deterministically — see decision log. Owner: `TODO(owner)` Pro price, domain, session boundaries.

## Next up

1. Owner decisions 2026-09-15 (design-decisions §17), now unblocked: (a) `drv.session` extractor on the five market-clock slots + `drv.day_session` composite field + `find.daysession` (tier 3, migration); (b) custom-field rule operands, owner-validated, + ADR (tier 3), which unblocks Module 04 stories 1.5–1.7 and non-`drv` graduation accepts; (c) Vercel Cron for the weekly review once the owner creates the Vercel project (`NEEDS_YOUR_INPUT.md`).
2. Repo-wide sweeps: Server Action `.strict()`, RLS FK ownership, `strategy_var` owner scoping.
3. Phase end: full suites, `/code-review`, `retrospeq-docs`. Then UI phase: restyle remaining ◐ rows in `brand/docs/inventory.md` (tier 1).
4. `retrospeq.detections` has no persisted per-user threshold/fence column — closing this would let `seq.trades_per_day`/`seq.daily_loss_breach`/`risk.spread` also resolve real rule proposals (`docs/infra-gaps.md`).

## Decision log

Newest first. **One entry ≤ 20 lines**, template in `.claude/skills/ledger/SKILL.md`. When this section passes ~40 entries, move the oldest to `docs/ledger/decision-log-<from>_to_<to>.md`.
- **2026-09-16 · ORCHESTRATOR (inline) · UI 1b qa FAIL fixed.** Rejection alerts (`CONNECT_CREDENTIAL_TOO_PERMISSIVE` and the generic one) moved above the `<fieldset>`, directly under the h1, matching frame 1.4 — they had rendered after every field, so a rejected trader had to scroll past the whole form to learn why (screenshot-confirmed by qa: `1.4-connect-rejected-light-top.png` showed the alert half-cut at the fold). tsc clean, 41/41 accounts tests, eslint clean. qa's other verdicts stand: row 1.3 ◐ is honest (`adapter.connect()` is one opaque call, a 4-step checklist would be fabricated), credential never echoed, keyboard pass clean on manual entry.
- **2026-09-16 · QA (design-audit) · UI batch 1b (9259deb, `/accounts/connect` rows 1.2-1.5 + `/trades/manual-entry` 1.6) · FAIL, pointable.** 1.2/1.4/1.5/1.6 restyled correctly: `.push` bottom-pin confirmed real via screenshot (`.connect`/`.entry` now in the `flex:1 1 auto` rule); one `.rq-btn` per view (success/connected states are separate branches); `.rq-num` on size/entry/exit/stop; no red/green (`.alert`/`.alert--blocking` are ink-only, `security-grep` clean); credential never echoed (state never carries it, screenshots confirm empty field post-rejection); 1.6 keyboard pass clean — only instrument/size/entry/exit/stop take typing (08 §5.6's own named fields), Direction is a real `.segmented` radiogroup. Row 1.3 ◐ reconciliation confirmed honest by reading `lib/broker/connect.ts:183-243`: `adapter.connect()` is one opaque call with no per-step signal to surface, so a real 4-step checklist would be fabricated — not a pointable fix. Row 1.5 ● manual-only caveat is disclosed in inventory + ledger, honest.
  **Real mockup-fidelity miss**: `accounts/connect/page.tsx:183-198` renders the rejection alert AFTER the full field set (platform/server/login/credential), but frame 1.4 (`home-onboarding.html#1.4`) puts the alert immediately under the h1, before any field — hierarchy inverted, alert requires scrolling past 3 field groups to reach (confirmed in `tmp/ui/batch-1b/1.4-connect-rejected-light-top.png`, alert only half-visible at bottom). Fix: move the alert block above the `<fieldset>`. Minor: mockup's "How to find it"/"Where to find your investor password" links omitted from both 1.2 and 1.4's copy.
  Tests: `app/(app)/accounts` 41/41, `app/(app)/trades` (excl. live) 67/67, tsc clean — matches ledger's "108 unit tests green" claim. Not committed (no changes made).
- **2026-09-16 · ORCHESTRATOR (inline) · ADR 0046 qa FAIL fixed: captured `pick_many` compared backwards.** `trade_captures.value` for a multi-select is the ARRAY the trader picked; `evaluate.ts`'s `compareSet` assumes a scalar (its only `pick_many` entry, `day_of_week`, extracts one day), so `in` never matched and `not_in` always did — a fabricated evaluation on every trade, reachable today via the Server Action even though no UI lists these operands yet. `evaluate-field-operand.ts` now applies set semantics for that one case (`in` = intersects, `not_in` = disjoint); `pick_one` and every other type still use the shared `compare()` untouched. New `lib/rules/__tests__/evaluate-field-operand.test.ts` (8 tests) asserts the COMPARISON — the gap that let this ship: every prior test covered parsing/catalogue/ownership only. ADR 0046 updated with the set-semantics section. tsc clean, 8/8 green. qa's other findings: archived-field gives the trader no visible signal on /rules (pre-existing freeze posture, now reachable via a self-service action) — logged, not fixed here.
- **2026-09-16 · CODER (agent, ledger written by orchestrator) · UI batch 1b `/accounts/connect` + manual entry.** Rows 1.2 ●, 1.4 ●, 1.5 ● (manual path only — no credentialed connect is reachable without a real broker), 1.6 ● (manual-entry), 1.3 ◐ (honest single-phase pending state; the frame's four-step auth→read-only→caps→import checklist needs per-step progress the connect flow doesn't emit). `.connect`/`.entry` added to the `flex: 1 1 auto` rule beside `.dash`/`.hook` so `.push` CTAs actually bottom-pin (the CSS that had never shipped, fixed earlier today). Screenshots read light + dark, `tmp/ui/batch-1b/` (14 PNGs incl. the rejection alert and a filled manual-entry form). Restyle only — no credential handling, Server Action or `lib/broker/**` change. tsc clean; 108 unit tests green across `app/(app)/accounts` + `app/(app)/trades`. Agent stalled on the 600s watchdog at its own ledger step; orchestrator verified and finished. Needs qa (design-audit).
- **2026-09-16 · CODER · UI phase batch 1b (`/accounts/connect` rows 1.2–1.5, `/trades/manual-entry` row 1.6) · tier 1.** `.explainer`/`.field`/`.segmented`/`.alert`/`.alert--blocking`/`.capability` are real, already-shipped classes (`components.css`, "Design program batch 6") — four files (`accounts/connect/page.tsx`, `ManualEntryForm.tsx`, `plan/page.tsx`, `AccountSettingsForm.tsx`) carried a stale "there is no such class" comment; fixed the two I touched, left the other two for whoever restyles those rows. Platform/Direction pickers switched from `.rq-pills` role=radio buttons to real native `.segmented` radio+label (frame's own markup); no e2e selector changes needed (`getByRole('radio', {name})` resolves either way).
  **Reconciliation, 1.3**: frame's multi-step `.verify` checklist implies real incremental progress; there's still no adapter to report it from (one Server Action round trip). Kept the prior slice's single-phase pending state, swapped heading/subtext to the frame's own copy while `pending` — honest, not a fabricated step list. Inventory 1.3 stays ◐.
  **`.push` bug, same class as batch 1a's qa finding**: `.connect`/`.entry` root classes added to `components.css`'s `.dash, .hook {flex:1 1 auto}` rule (both files, resynced to `public/brand/`); proved by screenshot (button pinned above the tab bar on 1.5/1.6 where content is short; on 1.4's taller rejected state — 6 platforms + full alert exceeds one 390×844 screen — content correctly overflows first, no overlap, confirmed scrolled).
  **Real bug found via screenshot, fixed**: `useActionState`'s `state` only updates on resolution, so a stale error from a PREVIOUS attempt rendered under a NEW pending submission's "Checking your account" heading. Both error blocks now gated `!pending &&`.
  1.5 "Connected" is only reachable via the manual path today (every credentialed connect fails at `CONNECT_KMS_NOT_CONFIGURED`, standing infra gap) — inventory notes this. Screenshots: `tmp/ui/batch-1b/` (11 PNGs, light+dark, both states of 1.6). `npm run verify` tier 1 clean. Not committed. Needs tester; no security/qa surface touched (scope was restyle-only, `lib/broker/connect.ts` untouched).
- **2026-09-16 · QA · custom fields as rule operands (07c40c8, ADR 0046) · FAIL, pointable.** No compound rules: `{operand_id,op,value}` unchanged, exactly one op key live per phrasing map (`field-operand-catalogue.ts`), one dispatch branch in `evaluate-field-operand.ts` — confirmed by read. Both judgment calls (rating `gte`+`lte`, `pick_many` `in`/`not_in`) match Module 03 §4.3's own op vocabulary and are ADR-documented. `check:import-boundaries` clean; 21+9+103 tests green (`field-operand-catalogue.test.ts`, `field-operand-resolver.test.ts`, `app/(app)/rules/__tests__/actions.test.ts`).
  **Real bug, reproduced directly (not caught by any test)**: a `pick_many` custom field's `trade_captures.value` is a JSON ARRAY (`captured-value-validation.ts`'s own non-empty-array check), but `evaluate-field-operand.ts` hands that array straight to `evaluate.ts`'s `compareSet`, which assumes a SCALAR observed value (built for `day_of_week`'s single-day-per-trade fact, the only prior `pick_many` precedent). `ruleValue.some(v => v === observed)` can never match an array. Repro (throwaway test, deleted): trade captured `['A','B']`, rule "never one of [A,C]" — should break (A is forbidden) — evaluates `followed:true` every time; `in`/`not_in` are permanently inverted for every pick_many custom-field rule, a fabricated evaluation, not an honest anomaly. Zero unit/live coverage exercises `evaluateFieldOperandRule`'s actual comparison against a captured value — only resolution/ownership is tested.
  Not reachable via the UI yet (`RuleEditor`'s picker never lists `field:` ids, commit's own "interim pre-gate"), but IS reachable today via `createRule`/`editRule` Server Actions directly against a Pro user's own pick_many field — live, not hypothetical.
  Separately, pre-existing-but-newly-reachable: a trader sees nothing on `/rules` or the adherence surface when an authored rule's field is archived (freeze-evaluations' existing silent-log-and-skip posture) — log as a follow-up for the story 1.5–1.7 UI slice, not blocking this backend-only commit.
  **Fix**: `evaluate-field-operand.ts` needs an array-aware comparison for `pick_many` (set intersection against `ruleValue`, not a straight `compareSet` delegation) plus a real test exercising a captured array value end-to-end. Not committed; no files changed.
- **2026-09-16 · SECURITY · custom fields as rule operands (07c40c8, ADR 0046) · PASS.** Not SQL/eval: all new files (`field-operand-catalogue.ts`, `-resolver.ts`, `evaluate-field-operand.ts`, `fields-repository.ts:1801`) use parameterized queries only (`$1..$3`), no id/op/value ever concatenated or eval'd; `fieldIdFromOperandId` rejects empty-suffix `"field:"`. Ownership both points: write (`create-rule-internal.ts:186`) and edit (`actions.ts` `editRule`) call `resolveFieldOperandForRule(userId,...)` → `fetchFieldForRuleOperand` (`withUserConnection`+`user_id=$1`); evaluate (`freeze-evaluations.ts:378`) passes `trade.user_id` from the trade row, never the session caller. Archived-after-authoring: `evaluate-field-operand.ts:74-92` maps only the 3 field-operand errors to `RuleEvaluationError` (anomaly, no `rule_evaluations` row, `freeze-evaluations.ts:380-397`); any other error rethrown, not swallowed — confirmed by read; the one permitted live test hit the known Tokyo-latency hook-timeout, environmental. Strategy-scope: `field-operand-resolver.ts:68-72` rejects `strategy_var` unless scope matches owner strategy; scopeId itself still checked by standing `isStrategyOwnedByUser` (49df6a0). Entitlement: no re-check of `fields.custom` for a `field:` operand — gate is that free tier (cap 0) can never own one, so direct-action call is honest `FieldOperandNotFoundError`, not bypass; **noted**: Pro→free downgrade leaves fields active, pre-existing pattern class, not introduced here. No new client-facing Zod fields, no new `withServiceRoleConnection` sites, no credential material logged. `check:security` full sweep not run this session (watchdog risk); findings are from direct code read + one live-test attempt.
- **2026-09-16 · ORCHESTRATOR · weekly notification scheduler built (owner decision §17, Vercel Cron).** `app/api/cron/weekly-review/route.ts` (nodejs runtime, force-dynamic, maxDuration 300) calls `runWeeklyReviewNotificationJobForAllUsers`; `vercel.json` schedules it Mondays 06:00 UTC. **Fail-closed auth**: unset/blank `CRON_SECRET` → 503 and the job does NOT run; wrong/missing header → 401, constant-time compare, no detail leaked. Takes zero caller input. Exactly-once stays where it belongs — `review_notifications`' unique `(user_id, period_start)` claim — so a duplicate delivery or manual re-trigger is harmless; no second weaker guard added here. Logs one tally line plus one line per failing user. 6 route tests (unset, blank, no header, wrong secret, wrong scheme, success tally, batch failure → 500). `docs/infra-gaps.md` Vercel entry closed; runbook entry for the 503; `NEEDS_YOUR_INPUT.md` now asks only for `CRON_SECRET` on Vercel. Nightly `operand_distributions` recompute still has no cron entry — left open deliberately.
- **2026-09-16 · CODER (agent, finished by orchestrator) · custom fields as rule operands · tier 3, ADR 0046.** `field:<field_id>` operand ids resolve per user: `field-operand-catalogue.ts` (pure parsing + type→ops/value mapping, no DB), `field-operand-resolver.ts` (the ONE ownership/state/scope check both pipelines call, `withUserConnection` + explicit user_id), `resolve-operand.ts` (write-time), `evaluate-field-operand.ts` (evaluate-time, re-resolves against the CURRENT row). No new `OperandType`, so `evaluate.ts`'s `compare()` and `validate-operand-op-value.ts` are untouched; entries are never added to the static `OPERAND_CATALOGUE` (a per-user field is not versioned with the codebase) — new `OperandGroup` member `field` instead. A field archived after authoring surfaces as the freeze path's existing `RuleEvaluationError` anomaly: logged loud, no `rule_evaluations` row, trade still confirms. Orchestrator finished it: the agent stalled 4× on the 600s watchdog (its own unit tests run in 232ms, so the stalls were stream-level, not the work). Added `GROUP_LABELS.field` (tsc FAIL), wrote ADR 0046 and `field-operand-resolver.live.test.ts`. Green: tsc, eslint, 148 unit (incl. 103 rules actions), 2 live (cross-user field id rejected; archived field stops resolving). Needs security ‖ qa.
- **2026-09-16 · ORCHESTRATOR (inline) · UI batch 1a qa FAIL fixes + sessions slice finished.** (a) `.push` had NO CSS anywhere in the shipped app — 35 uses across `brand/docs/screens/*.html`, inert in every screen since the shell was built; added `.push{margin-top:auto}` + `flex:1 1 auto` on `.dash`/`.hook` in `brand/css/components.css`, resynced `public/brand/`, and rendered the real shell chain under Playwright to confirm (button top 593/844, screenshot read). (b) Closeout list sorted oldest-first in `app/(app)/dashboard/page.tsx` (display-only copy; the shared repository order is unchanged for its other callers). (c) `DashboardGroupingChip` had zero coverage — new `app/(app)/dashboard/__tests__/DashboardGroupingChip.test.ts` (6 tests; note `.tsx` is NOT in vitest's include glob, hence `.ts` + `createElement`). (d) Sessions slice: coder stalled 4× on `supabase db push`'s interactive confirm — migrations applied via a throwaway pg script instead; wrote `session-fields.live.test.ts` myself, bulk-seeding via `generate_series` (per-trade loop: 20 min and a blown cleanup hook; bulk: 9s). Live-proven: 10:00 UTC → London, 18:00 UTC → New York, composite `Wed · London`. `findings.segment` is jsonb `{op,value}`, not a bare label.
- **2026-09-16 · QA (design-audit, tester folded in) · UI batch 1a (65ccda7, rows 1.14/1.17/1.9/1.10/1.11/3.6) · FAIL, pointable.** R-track math (`format.ts:rTrackFill`) correct: null→no fill (honest), 0→real zero-width fill distinguishable in code from null, clamp≤100 pre-halving so fill never passes the half-track — all proven by `format.test.ts` (9/9 real). No currency, one `.rq-btn` per state (0 on open, matches decision doc), `.rq-num` on times/counts, grouping chip reuses `resolveAmbiguousGroupingAction`, no new write path — all PASS.
  **2 real mockup-fidelity misses, both reproduced via the existing screenshots** (no dev server started): (a) `page.tsx`'s closeout list renders `listClosedUnconfirmedTrades`'s `closed_at desc` order (`trades-repository.ts:54`) — newest-first — against frame 1.14's explicit oldest-first read (09:14→14:02); pre-existing query, newly exposed by this restyle, never disclosed. (b) `.push` (`page.tsx:495,`onboarding/hook/page.tsx:101`) has **zero CSS rule** in any shipped stylesheet — `margin-top:auto` exists only in the mockup harness's own `brand/docs/screens/frame.css`, never ported to `retrospeq-design-system/brand/css/*.css`/`public/`/`app/brand-tokens/`; even if ported, `.dash` has no `flex:1` to create free space. Confirmed in all 3 screenshots: CTA sits directly under content, not pinned to bottom. Pre-existing (pre-dates this diff, e.g. line 412/591), but this diff adds 2 more instances and its own comments assert bottom placement that doesn't render — inventory rows 1.9/1.14 marked ● overclaim.
  **Real gap**: zero test (unit or e2e) for new `DashboardGroupingChip.tsx` — nothing proves the ambiguous-only render, the real write, or the `/trades#trade-<id>` link.
  **Fix**: (a) sort/reverse for this display only; (b) add `.push{margin-top:auto}` + `flex:1` up the `.dash` chain (design-system-level, log as its own ticket) — not committed.
- **2026-09-16 · CODER · UI phase batch 1a (Home + onboarding restyle, rows 1.8/1.9/1.10/1.11/1.14/1.17/1.19/3.6) · tier 2.** 1.14: `.rq-row`+`.rq-track`/`.rq-fill` real R marks (`trades.r_multiple`, new `rTrackFill`/`formatDirectionLetter`); fixed a real overflow bug found via screenshot (`pct` is vs the HALF-track, CSS width needed `pct/2`). 1.17: new `DashboardGroupingChip.tsx` on the open-position card, real `groupingConfidence` pass-through, reuses `/trades`' own `resolveAmbiguousGroupingAction`; "Separate" deep-links to `/trades#trade-<id>`. Swapped the `open` state's list off `.dash__trades` (a row shape) to flex-column so the chip stacks under the card, not beside it. 1.9: `.push`/`.rq-btn--block`; frame's "About 30 more trades" block omitted, no real source pre-calibration. 1.8 stays ○, reason updated. **1.10/1.11/3.6: stale-inventory finding, not new work** — `GuidedFrontDoor.tsx` was restyled 2026-09-14 (`daa5e46`), inventory authored 48 min earlier, never revisited; screenshot-verified both variants for the first time (seeded a real `operand_distributions` row for "with history" — that path reads a materialised table, not `trades`). Kept the existing `.rq-btn--equal` Add/Skip pair over the frame's single "Start" — a prior, documented ethics call, not relitigated. Verify: tsc/eslint/unit/live pass (one flaky run mid-session, clean on rerun — connection-pool exhaustion, not code); `e2e:changed` 7/7 (2 selectors updated for real markup changes). CSS: `.grouping-chip__actions` also styles `a` now (resynced). Needs tester; no security/qa surface touched.
- **2026-09-16 · VERIFY · First full run against the new Supabase project.** `check` 2745/2746 (1 test assumed pre-existing users; now asserts the derived-id subset, seeding still proven by a real signup). `check:security` 317 pass live. `check:live` 636 pass / 9 fail: 7 timeouts from latency (Tokyo DB, 112 ms/round-trip from India; Mumbai measures 28 ms), 1 real bug (`listTradesForAccountDay`/`listOpenTrades` ordered only by `opened_at`, ties arbitrary; now `, id`), 1 test/pooler mismatch (session-level advisory lock across statements; local `SUPABASE_DB_URL` now session pooler :5432, Vercel stays 6543). Both fixed files pass live (15/15). ~13 other untied timestamp orderings logged (rule-engine ones need a tier-3 audit). Final `verify` not clean: a second session (`retrospeq-v1-ea`) ran the live suite on the same DB concurrently, and my test-user cleanup deleted its in-flight users (told it). Region recreate (Mumbai) pending owner decision.
- **2026-09-15 · DECISION · Own Supabase project + Vercel environments (ADR 0045, supersedes 0002).** Shared LuceEdge dev project deleted by owner (dev data only). New free project `Retrospeq-v1` (`qvsqkciaewkbgaqgieeb`, Tokyo) serves local dev + Vercel Production until a prod project is created ~a week before alpha. `supabase link` + `supabase db push`: first from-scratch replay failed — `shadow_harness` (20260819020000) referenced `profiles` (20260820010000); it had only worked on the shared project because it was hand-applied. Renumbered to `20260820015000` (no DB had the old version recorded; content unchanged, comments/refs updated). All 33 now recorded; RLS on all 50 tables; the 2 no-policy tables (`erasure_tombstones`, `rate_limit_hits`) are deliberate server-only. Advisor: 17 invoker trigger fns lack pinned `search_path` (infra-gaps). Vercel: project `retrospeq-v1`, `main`→`app.retrospeq.com`, GitHub linked, Supabase integration set Production vars (sensitive/write-only). `.env.local` repointed (URL, anon, service role); **`SUPABASE_DB_URL` blank until owner supplies the DB password**. Blocked by permission classifier, owner to do: auth SMTP/site URL/redirects/password-min-8 on the new project, Vercel protection → All Deployments. Live suites not yet re-run on the new DB (needs `SUPABASE_DB_URL`).
- **2026-09-15 · OWNER · NEEDS_YOUR_INPUT settled · —.** Recorded in `retrospeq-design-decisions.md` §17 "Closed this session" (authoritative wording there). (1) Sessions: market local clocks, five slots (Asia / London / London–NY overlap / New York / Off-hours), DST via IANA zones; account rollover sets the trading day only. (2) Day × session: one composite derived field `drv.day_session`, retroactive; edge engine stays single-field. (3) Custom fields may be rule operands: owner- and type-validated, still `{operand_id, op, value}`, rejected at write and evaluate if unknown or unowned; tier 3 + ADR. (4) Scheduler: Vercel project + Cron; owner creates the project, so that's the only remaining `NEEDS_YOUR_INPUT.md` entry. Not yet decided: `TODO(owner)` Pro price, domain; `find.session` beta→live promotion stays on the §4 process.
- **2026-09-15 · ORCHESTRATOR · the "analytic_user_suppression flake" was never a flake.** `analytics-registry-schema.independent-verify.rls.test.ts` drops two `analytic_config` CHECKs and revokes `select ... from authenticated` on `analytic_user_suppression` mid-assertion; in parallel that fails every other file touching those objects (20 failures in one `vitest run rls.test` sweep), and a killed run had left `analytic_config_min_account_tier_check` PERMANENTLY dropped on the shared dev project (restored by hand, verified). Fixes: file excluded from parallel runs (`vitest.config.ts` AND `check`/`check:security`'s own `--exclude` flags — a CLI `--exclude` REPLACES the config list), run alone via `npm run test:exclusive` (`vitest.exclusive.config.ts`, `fileParallelism: false`); `restoreSchemaGuards()` is idempotent and also runs in `beforeAll`, so a killed run self-heals; `drop constraint if exists`; 1c's race loop cut 12→4 trials with a 60s timeout (measured: ~5s per `canRender` because each opens a fresh pooled connection to the remote project — 69s against a 30s timeout); global `hookTimeout: 30_000` for live seed/cleanup hooks. **`npm run check` now: 2746 passed + 7 exclusive, 0 failed.**
- **2026-09-15 · DOCS · Phase-end refresh of `docs/DEVELOPMENT.md`.** Rewritten against the current repo tree (Modules 01-08 now feature-complete for reachable scope; `lib/review/`, `lib/engagement/`, `lib/analytics/{edge-engine,detection-engine}` all new/grown since the last refresh), 33 migrations, 43 ADR files (0001-0044, 0028 unused), `docs/infra-gaps.md`/`NEEDS_YOUR_INPUT.md`. **Found and corrected two stale claims in this file's own Phase-status table**: the silent default strategy (§5.4) and the four-state dashboard (§7.1) are both real and wired (`lib/onboarding/default-strategy.ts`, `lib/dashboard/dashboard-state.ts` — read directly, not assumed), not "left" as the dated table row states — noted with reasoning in DEVELOPMENT.md's Module 08 section rather than silently overwritten here. Pruned all Windows-host gotchas (host moved to macOS/Node 24, per AGENTS.md/process.md) and stale test-count/coverage figures. Not committed.
- **2026-09-15 · TESTER · Phase-end gap: Module 06 decision-flow E2E specs (promotion/retirement/detection/monthly review) · PASS.** `e2e/review-decisions-promotion-retirement.spec.ts` (promotion accept/decline; retirement `.rq-btn--equal` core flow + a blocked-premise skip proof — `canDecide:false` is unreachable via the real route, `actions.ts:378` filters server-side; tested the real reachable case instead: a rule retired between materialisation and view is silently skipped). **Collision**: another agent already committed a near-byte-identical file at this same path (a625fa0) — my working-tree diff on top is only generous-timeout tuning (below), zero logic change; orchestrator to reconcile/commit. New `e2e/review-decisions-detection.spec.ts` (accept on now-real `seq.reentry_after_loss`; `risk.spread` → "can't become a rule yet", "Not yet" is a DEFER not decline per the code's header — dispatch said "decline", corrected) and `e2e/review-month.spec.ts` (populated 3-month panels + honest empty state), neither pre-existing. All 8 passed individually with real DB assertions; fixed one seeding bug (`fetchStrategiesForUser` inner-joins `strategy_versions`). **Environmental, not code-caused**: batch runs showed heterogeneous timeouts/ECONNRESET/one non-reproducing FK violation — pg latency normal (~130ms), no locks, Supabase's own Auth Admin API 500'd on every delete during my cleanup. Cleaned 45+ orphaned users' DB rows from crashed runs; ~14 `auth.users` rows need a `test:user -- cleanup` retry once the platform recovers. Dev server stopped. Not committed.
- **2026-09-15 · SECURITY (re-verify) · P1 fix 49df6a0, narrow.** PASS. Every `scope='strategy'` write funnels through `createRuleInternal` (grepped: `createRule`, `acceptGraduationDecision` — the only two non-test writers of `scope:'strategy'`), which now checks `isStrategyOwnedByUser` before any other read. `editRule` takes no `scope`/`scopeId` param at all — cannot change scope, confirmed by signature; `current.scope`/`scopeId` come from `fetchCurrentRuleForEdit`, already `withUserConnection` + `user_id`-filtered. `isStrategyOwnedByUser`: same double-layer pattern, correct. Every reachable caller Zod-guards `scopeId` as `z.uuid()` before it arrives (`createRuleInputSchema`, `graduationEvidenceSchema`) — no raw-string-to-pg path found. TOCTOU: `rules.scope_id` has no FK by design (pre-existing, documented polymorphic-reference decision, migration 20260823020000) — check and insert are two separate transactions, app-only backstop; worst case is a dangling `scope_id` on a delete-race, not a cross-user leak (ownership already passed). Same severity class as the two already-tracked P2s; narrowest real fix would be folding the ownership check into `insertRuleAndVersion`'s own guarded INSERT (single transaction) — not done here, not blocking. `rules/new/page.tsx`'s stale "Module 03 doesn't exist" header comment is still present (unrelated to this fix — that screen never sets `scope:'strategy'`). Tests: unit 103/103, live `isStrategyOwnedByUser` 1/1 pass.
- **2026-09-15 · ORCHESTRATOR (inline fix) · security sweep P1.** `createRuleInternal` (`lib/rules/create-rule-internal.ts`) now rejects `scope='strategy'` unless `scopeId` is the caller's own strategy (`isStrategyOwnedByUser`, new in `rules-repository.ts`, `withUserConnection` + explicit `user_id`), returning `STRATEGY_NOT_FOUND` before any tier/entitlement read. Covers every caller of the pipeline (`createRule`, review decision accepts). Tests: unit (`app/(app)/rules/__tests__/actions.test.ts`, 103/103, unowned id → no insert, no entitlement read) + live cross-user (`rules-repository.live.test.ts`, passes; unrelated 15s timeouts in the same file on a slow shared DB pass when rerun alone). Sweep P2s (strategy_var owner cross-check, decisions write-before-guard race) stay in phase-end list. Needs security re-verify only.
- **2026-09-15 · SECURITY SWEEP · Repo-wide audit (Server Actions, RLS FK ownership, `strategy_var` scoping) · 1 P1, 2 P2 (both already tracked), 0 P0.** `check:security`: 324/324 (retry; `analytic_user_suppression` permission-denied is the documented pre-existing flake, gone on rerun), import-boundaries clean, security-grep clean — its `.strict()` WARN at `app/(app)/rules/actions.ts:104` is a false positive (comment text, not a schema; the real schema at :131 uses `.strictObject`), verified by read.
  **P1 (new, verified by read)**: `createRule`/`createRuleInternal` (`app/(app)/rules/actions.ts:248`, `lib/rules/create-rule-internal.ts:156`, `lib/rules/rules-repository.ts:456-465`) accepts client-supplied `scopeId` for `scope:'strategy'` with no ownership check that the strategy belongs to the caller — `rules/new/page.tsx`'s own comment claiming this is unreachable is stale (Module 03/strategies shipped 2026-09-09; Server Actions are network-reachable regardless of UI). No cross-user read/display path found (`scopeId` never joined to a strategy name anywhere I could find), so blast radius is same-user junk-rule/entitlement-cap waste, not a data leak — but it's an uncatalogued instance of the exact standing gap in `docs/infra-gaps.md:37`. Fix: `assertStrategyOwnedByUser`-style check before insert, matching `lib/fields/fields-repository.ts:433`'s own precedent for `ownerStrategyId`.
  **P2, confirmed still open, already tracked**: `docs/infra-gaps.md:44` (`strategy_var` `owner_strategy_id` vs `field_usages.used_by_id`, same-user only) and `:17` (review-decision domain-write-before-guarded-prompt-update race, same-user only).
  **N/A**: `arm_events` "for all" RLS has no FK check on `matched_trade_id`, but zero client write path exists (feature not built) and `retrospeq` schema isn't PostgREST-exposed (`docs/infra-gaps.md:48`).
  **PASS (by read)**: every Server Action derives user from session, never client input; ownership checks present for fields/strategies/trades/trade_captures (`assertStrategyOwnedByUser`, scoped `fetchFieldDefinitionsByIds`, inline trade-ownership checks, `fetchStrategyVersionFields`); rate limiting on every auth/connect/mutating action; `lib/broker/connect.ts` read-only verification has no bypass, KMS failure propagates; no vendor type outside `lib/broker/`; test-bypass flag read only by `limiter.ts`; no eval/new Function; service-role-inventory test passes.
- **2026-09-15 · ORCHESTRATOR (inline fix) · Slice 10c QA FAIL.** Added idempotent `openCatalogue(page)` (sets `details.catalogue.open = true`, never toggles) before every `#operand-picker` `selectOption` in `e2e/rules-general-editor.spec.ts` + `.independent-verify.spec.ts`. `npx playwright test` both specs: 11/11 pass (1.9m; `destination stream closed early` WebServer lines are aborted-navigation noise). Test-only change proving QA's exact finding fixed → closed without a second QA dispatch (proportional-gates rule). Dev server stopped.
- **2026-09-15 · QA (tester folded in) · Module 04 discovery (Slice 10c, frame 3.10, `/rules/new`, 42406fb) · FAIL, pointable.** Both judgment calls accepted: (a) "Based on your last 90 days" (real `DETECTION_WINDOW_DAYS`) over the mockup's illustrative "90 trades" — honest, matches "observation not diagnosis"; (b) skip (not mark) an already-governed operand — matches `guided-front-door.ts` precedent. Copy/evidence real (`occurrences`), `.rq-num` on evidence, no red/green, honest empty state, one `.rq-btn` (discovery items are plain `.discovery__btn`), pre-fill seeding matches established "seed from history, preview decides" posture, free-tier cap shows the pre-existing plan-limit copy (not a new dead end, own test covers it) — all PASS. Rendered vs frame 3.10 (`renderToStaticMarkup` + real compiled Tailwind CSS + Playwright, populated/empty/catalogue-open, dark) — layout/hierarchy/marks match, deleted after. `check:import-boundaries` clean.
  **Real regression, confirmed by isolated repro (not taken on faith)**: the catalogue's `<select id="operand-picker">` now sits inside a closed-by-default `<details class="catalogue">` with no `open` prop or JS toggle — native `<details>` content is `display:none` until opened. `e2e/rules-general-editor.spec.ts` (5 tests) and `e2e/rules-general-editor.independent-verify.spec.ts` (6 tests) call `page.selectOption('#operand-picker', ...)` directly, never opening the disclosure first — Playwright's actionability check requires visibility, confirmed via a minimal standalone repro (`page.selectOption` times out on a closed `<details>`). Coder's own ledger entry never mentions running `e2e:changed` for this route despite its behaviour visibly changing (dispatch itself flagged this exact risk). Unit/live/render tests are behaviour-based and adequate for the new discovery code but cannot catch this since they don't touch the pre-existing E2E specs.
  **Pointable fix**: open the `<details>` (e.g. `page.locator('.catalogue summary').click()`) before every `#operand-picker` interaction in both spec files, then run `npm run e2e:changed` for real. Not committed; tree left clean.

- **2026-09-15 · CODER · Module 04 discovery (Slice 10c, frame 3.10, `/rules/new`) · tier 2.** New `lib/review/discovery.ts`: `rankDiscoveryItems` (pure) + `fetchDiscoveryForUser` (I/O) — composes `lib/analytics/detections-repository.ts`'s `fetchActiveDetectionsForUser` with `lib/review/decisions/detection-operand-map.ts`'s existing `resolveDetectionRuleProposal` (the SAME mapping Module 06's frame 4.10 uses, not re-derived), gated on `ruleProposable`, `getEditableOperands` membership (type + tier), and NOT already governed by an active global rule (`fetchActiveGlobalRuleVersionsForOperand`, same read `guided-front-door.ts` uses for its own three seeded operands). Ranked by `occurrences` desc, tie-broken by `analyticId`.
  **Reconciliation #1**: the reference markup's heading example ("Based on your last 90 trades") does not match how `detections` is computed — `DETECTION_WINDOW_DAYS` (`gates.ts`) is a 90-CALENDAR-DAY clock window, not a trade count. Rendered heading says "your last 90 days" instead of inventing a trade count nothing tracks (dispatch's own instruction).
  **Reconciliation #2**: dispatch left "skip vs mark already-in-rulebook" open — chose SKIP (not mark), matching `guided-front-door.ts`'s own precedent for its three seeded operands; a trader already holding a rule for an operand gets no value from being told to consider it again.
  `RuleEditor.tsx`: discovery renders as plain `.discovery__btn` list buttons (never `.rq-btn`) above the picker, now behind `<details class="catalogue">` + a client-side label-substring search (a FORM screen, keyboard allowed). Clicking an item pre-fills the stepper at the analytic's own resolved threshold via a new shared `roundToBoundsStep` helper (refactored out of the existing `boundsMidpointDefault`).
  Tests: 10 unit (`discovery.test.ts`, no-detections/unauthorable/governed/ranking/tie-break), 4 live (`discovery.live.test.ts`, owner-scoped incl. cross-user isolation, direct-insert `detections` fixtures per `eligibility.live.test.ts`'s own established posture), 5 page-render (`page.test.ts`, mocks `../../actions` by RuleEditor's own resolution path — a test-location gotcha worth remembering). `npm run verify` tier 2 clean (tsc/eslint/unit/live), `check:import-boundaries` clean. Render-checked vs frame 3.10 (`brand/docs/screens/rulebook.html#3.10`) via `renderToStaticMarkup` + a throwaway `tsx` script (Module._load patch for `server-only`, deleted after) + Playwright, light theme only — matches layout/copy/marks. Inventory 3.10 ○→◐. Not committed. Needs tester ‖ qa.
- **2026-09-15 · QA · Re-verify: cross-trade operand `computableToday` flip + detection-map reconciliation (d2b7b76) · PASS.** Diff only re-flags metadata (`operand-catalogue.ts` factNotes/flags, `detection-operand-map.ts` header+2 branches); `cross-trade-operand-values.ts`/`freeze-evaluations.ts` untouched. Spot-checked vs spec: `time_since_last_loss` null (never 0) with no prior loss, matches §5.3 step 4; `trades_today`/`daily_loss_pct` scope to `server_day`/ISO week, matches §5.4 "attach to the trade that crossed the line"; `consecutive_losses` breaks on scratch same as win — documented judgment call, spec silent, not a violation. No `rule_evaluations` writes/backfill (no migration/UPDATE in diff). `check:import-boundaries`/`tsc`/`eslint` clean; reran `page.test.ts`+`detection-operand-map.test.ts`+`computable-operand-values.test.ts` (54/54). Grepped `computableToday` consumers: `preview.ts` gates on `DISTRIBUTION_OPERAND_IDS` (unaffected), `editable-operands.ts` doesn't gate on it, no app route reads it — no rule-editor/catalogue/calibration ripple. Frame 4.10: read `tmp/dev-screenshots/detection-decision-ready-{light,dark}.png` (still on disk, gitignored — ledger's "deleted after" is stale but harmless) vs `review-performance.html#4.10` — layout, copy, one `.rq-btn`+one ghost, `.rq-num` all match; mockup's illustrative "5 minutes" vs rendered "2 minutes" is the real threshold-derived value, expected. `canAccept:false` branch still renders honest "can't become a rule yet," never hidden/zeroed. `docs/infra-gaps.md` line accurate. Not committed.
- **2026-09-15 · SECURITY · Tier-3 review of d2b7b76, cross-trade operand `computableToday` flip · PASS.** `editable-operands.ts` (read, confirmed) never gates on `computableToday` — the general rule editor's offered operand set is unchanged, and per-operand `bounds`/type validation (`validate-operand-op-value.ts`) is unaffected (bounds untouched in diff). `preview.ts` gates on `DISTRIBUTION_OPERAND_IDS`, not this flag. So the flip only widens `graduation-operand-map.ts`/`detection-operand-map.ts`'s honesty gate.
  `resolveDetectionRuleProposal` derives `value` only from static constants (`REENTRY_THRESHOLD_SECONDS`→2min, `CONSECUTIVE_LOSS_STREAK_THRESHOLD`→2), clamped to the operand's own catalogue `bounds` — never client-supplied. `acceptDetectionDecision` (`actions.ts:1323`) re-resolves the proposal live server-side (never trusts rendered `canAccept`), then `createRuleInternal` independently re-runs operand whitelist, tier gating, and the free-tier `rules.create` cap (3, `capability-table.ts:28`) via `canForUser` — confirmed this cap is a plain count, not restricted to specific operand ids ("three calibrated rules" is Module 08's onboarding default-seed screen, not an authoring restriction — checked `08-onboarding-and-home.md`). Both newly-reachable operands are `tier: 't0'` and `analytics.detection` is free-true for both plans, so no Pro-only-rule bypass for free users, and detection correctly needs no separate entitlement gate.
  `cross-trade-operand-values.ts`/`freeze-evaluations.ts` are unchanged by this diff (not in file list) — spot-checked anyway: every query scoped to `account_id` (file's own header), `accountId`/`userId` sourced from the trade row fetched by `tradeId` (`fetchTradeForFreeze`), never a client-supplied FK. Mock removals in `eligibility.live.test.ts`/`decisions-detection-integration.live.test.ts` verified to now exercise the real, unmocked function. `check:security` 323/324 — sole failure `analytic_user_suppression` permission-denied, matches dispatch's documented pre-existing flake. Not committed.
- **2026-09-15 · CODER · Cross-trade operand `computableToday` flip + detection-map reconciliation · tier 3.** Re-verified `cross-trade-operand-values.ts`/`freeze-evaluations.ts` end-to-end (not taken on faith) — all 20 built cross-trade operands are genuinely computed and merged into `TradeFacts` at freeze, with correct rollover/week-boundary handling (`server_day`/`week-boundary.ts`) and honest `null` exactly where no legitimate value exists (no prior trade/loss, equity unknown, trade still open) — never a fabricated 0/false. Flipped all 20 to `computableToday: true` in `operand-catalogue.ts`, each `factNote` naming the real function; left the genuinely-deferred 10 untouched. `detection-operand-map.ts`: header updated to reflect that `seq.reentry_after_loss`/`seq.consecutive_losses` now resolve for real (not hypothetical); the other 3 still `null` for a DIFFERENT, unchanged reason (no persisted per-user baseline/threshold on `retrospeq.detections`, a schema gap, not an operand gap) — code logic itself needed no change, already correctly gated.
  Removed now-unneeded module mocks of `resolveDetectionRuleProposal` in `eligibility.live.test.ts` and `decisions-detection-integration.live.test.ts` (both now exercise the real function); updated `review-prompts.live.test.ts`'s "REAL TODAY" test to use a real still-null analytic (`seq.trades_per_day`) instead of a deliberately-mangled id; deleted `detection-operand-map.derivation.test.ts` (its forced-mock is now redundant, folded into `detection-operand-map.test.ts` against the real catalogue). Fixed `computable-operand-values.test.ts`'s stale "8 == every computableToday operand" assertion to scope to the single-trade subset. Added 2 new `page.test.ts` cases (frame 4.10's now-reachable `canAccept: true` branch) + a throwaway static-render Playwright screenshot (light/dark) vs `review-performance.html#4.10`, deleted after. Confirmed `editable-operands.ts` doesn't gate on `computableToday` at all (pre-existing, unaffected either way) and `preview.ts` gates on `DISTRIBUTION_OPERAND_IDS` not this flag (unaffected, per that file's own header).
  Fixed a real pre-existing `scripts/verify.mjs` bug found mid-slice: `git diff --name-only` includes deleted files, crashing eslint with "no files matching pattern" — now filters to `existsSync`.
  Verify: tsc/eslint clean; unit 815/815 in touched dirs; live (run directly + via verify's `vitest related`): `cross-trade-operand-values.live` 9/9, `freeze-evaluations.live`+`independent-verification.live` 18/18, `eligibility.live` 9/9, `decisions-detection-integration.live` 5/5, `review-prompts.live` 11/11 — all real end-to-end proofs, not mocked. `check:security` 324/324, import-boundaries clean. `verify`'s one failure (`decisions-relaxation-integration.live` CROSS-KIND) is confirmed pre-existing via git log (test last touched 5481e33, `actions.ts`'s `plan_required` gained a required `kind` field afterward in later commits — the test's own `.toEqual` was never updated), untouched by this diff, reproduces in isolation. Not committed.
- **2026-09-15 · SECURITY · Narrow re-verify: CSV formula-injection fix (a94a340 on 2d96c17 FAIL) · PASS.** `escapeCsvCell` (`lib/privacy/export-csv.ts:58-67`) now guards every cell by default, exempting only `PLAIN_NUMBER` (`/^[+-]?(\d+\.?\d*|\.\d+)([eE][+-]?\d+)?$/`); confirmed unexploitable — the regex is fully anchored, so any payload combining a leading sign with formula content (`-1+cmd`, `+1+1`, `-1;cmd`) fails to match and stays guarded, verified against 25 hand-built payloads incl. exponent/spacing edge cases. jsonb scalars and objects, README, and TRUNCATED markers all now route through `escapeCsvCell` (confirmed by read, not just test). Live path (`export-job.ts`) calls only `buildFullExportCsv`; `export.ts`'s `tradingAccountsToCsv` (unguarded local `escape`) is dead code — only its own test imports it, not reachable from the export job — flagged as a latent trap, not a live vuln. `export-csv.test.ts` 28/28, `export-completeness.live.test.ts` 6/6.
- **2026-09-15 · CODER (orchestrator inline) · export CSV · security FAIL + qa pointable fix.** `escapeCsvCell` now guards EVERY cell by default (leading `= + - @` tab/CR → `'`), exempting only plain numbers (`PLAIN_NUMBER` regex, so `-1.5000` stays intact); the 5-column `freeTextColumns` allowlist is removed from `export-tables.ts`/`export-csv.ts` (it missed user-typed `instrument` and `trade_captures.value` option labels, incl. jsonb scalar strings). README + TRUNCATED marker lines now go through `escapeCsvCell` (commas no longer split them across columns); README de-jargoned. Tests: regression for instrument/value, plain-number exemptions, jsonb scalar string, marker/README quoting — export-csv/tables/job 42/42, export-completeness.live 6/6, tsc + eslint clean. Needs security re-verify only (qa PASS stands).
- **2026-09-15 · SECURITY · Narrow re-verify: CSV export formula-injection guard (2d96c17) · FAIL.** RFC4180 quoting correct; denylist + cross-user isolation hold — reran `export-completeness.live.test.ts`, 6/6 pass incl. the 2 CSV cases. But the guard's 5-column allowlist (`ExportTableSpec.freeTextColumns`) misses reachable user-controlled strings: (1) `trade_captures.value` (jsonb) — a `pick_one`/`pick_many` option label is user-typed, no charset check (`lib/fields/field-validation.ts:402-410`), stored verbatim; `cellToText`'s (`export-csv.ts:35-43`) "jsonb self-quotes" assumption is false for a jsonb scalar string — `pg` JSON.parses it to a plain JS string, missing the `typeof value === 'object'` branch, e.g. an option named `=1+1` passes through unguarded and unquoted. (2) `instrument` (`fills`/`trades`/`arm_events`/`position_snapshots`, bare `text`, no CHECK) — user-typed at manual entry (`lib/ingestion/manual-entry.ts:201-205`, `.max(40)`, no charset check) or broker-supplied, absent from every table's `freeTextColumns`. Neither is numeric/uuid/timestamp wrongly excluded — confirms guard-by-default (except proven-safe columns) is the safer design. Non-blocking: `## <name>` markers are plain text, not CSV fields — an embedded `\n## fake` line stays inside its quoted field for any RFC4180 parser but is visually confusable to a naive reader of one's own export; no cross-user/re-ingestion path exists, so not a boundary issue. Unrelated pre-existing flake: `analytic_user_suppression` permission denied in `check:security`. Send back to coder.
- **2026-09-15 · QA · Re-verify: privacy export CSV completeness (2d96c17 on 589807c FAIL) · PASS with a pointable fix.** (1) CSV now covers trades/fills/rules/rule_versions/rule_evaluations/strategies/fields — matches `/privacy` + `export-ready.html`'s promise. (2) Usability: generated a real sample via `buildFullExportCsv` (small hand-built bundle, deleted after) — data sections quote/escape correctly and open as legible per-section tables; ragged column counts across sections are harmless in a spreadsheet. Single multi-section file (vs. zip/40 signed URLs) is an acceptable, documented tradeoff — PASS on the architecture. **Real bug found**: `## README` and the `TRUNCATED` marker line (`export-csv.ts`, `buildCsvSection`) are pushed as raw strings, never run through `escapeCsvCell`/`csvLine` — README (6 commas) splits across 7 spreadsheet columns instead of one message; a truncated marker (1 comma) does the same, undermining the truncation-visibility guarantee exactly when a 50k-row cap is hit. Data rows unaffected. Contradicts this commit's "quoting on every cell" claim. (3) Truncation flagged, never silent, modulo that cosmetic split. (4) `docs/infra-gaps.md` line is honest re prior dishonest closure. Ran `export-csv.test.ts`: 23/23 pass (no test covers the raw-line gap). **Pointable fix, one line each**: wrap `README`/`marker` through `csvLine([...])`. Not committed; temp files deleted; tree clean.
- **2026-09-15 · CODER · Fix QA FAIL: privacy export CSV completeness (589807c follow-up) · tier 3.** JSON side untouched, per dispatch. New `lib/privacy/export-csv.ts`: `buildFullExportCsv` iterates `EXPORT_TABLE_REGISTRY` directly (can't drift from JSON side again), emitting one `## <table> (n=…[, TRUNCATED])` CSV section per table (+ profile/tradingAccounts/subscription/mfa + a leading `## README` section) into a SINGLE `export.csv` — chosen over a real per-table ZIP (no zip dependency exists in `package.json`, adding one unlogged would violate the dispatch; hand-rolling a ZIP writer is new unaudited binary-format code) or one-signed-URL-per-table (~40 links, a `/privacy` UI regression). Logged in `export-csv.ts`'s own header. `ExportArtifactManifest`/UI unchanged (still one `csvUrl`).
  RFC 4180 quoting on every cell (`"`,`,`,CR/LF); formula-injection guard (`= + - @`/tab/CR → prefix `'`) applied ONLY to new `ExportTableSpec.freeTextColumns` (fields.name, strategies.name, strategy_versions.name, trigger_conditions.text, rule_versions.rendered — checked against real migration DDL) — deliberately NOT applied to numeric-as-string columns (`r_multiple` etc., which legitimately start with `-`). jsonb columns JSON.stringify'd (self-quoting, `"` never `=`, so never need the guard). `truncated` flagged per-section, never silent.
  `export-job.ts` now uploads `buildFullExportCsv(bundle)` as `export.csv` (was `tradingAccountsToCsv`, kept unchanged/exported for back-compat + its own tests). Fixed the stale "Once Module 02 exists" comment on `tradingAccountsToCsv`. `docs/infra-gaps.md`'s 589807c-closure line corrected in place (names the JSON-only dishonesty, then re-closes for real with this fix) rather than silently re-closed.
  Tests: new `export-csv.test.ts` (23/23 — escaping, injection guard incl. negative-number non-corruption, per-table section generation, jsonb serialization, denylist-by-omission); `export-tables.test.ts` +2 (freeTextColumns validity); `export-job.test.ts` updated (mocks `export-csv` now, not `tradingAccountsToCsv`); `export-completeness.live.test.ts` +2 live tests extending the existing denylist/isolation proofs to CSV output (6/6 pass, ~49s). `npm run verify` tier 3: tsc/eslint clean, unit 133/133 (`lib/privacy`), live 324 passed/15 skipped, `check:import-boundaries` clean, `check:security` bundle ok (only the pre-existing documented `.strict()` gap warning, unrelated). Not committed.
- **2026-09-15 · QA · Privacy export completeness (589807c) · FAIL, pointable (not a product-intent decision).** JSON side is real and live-proven: ran `export.test.ts`/`export-tables.test.ts`/`export-job.test.ts` myself (21/21 pass) plus `export-completeness.live.test.ts` against the shared dev DB (4/4 pass, ~32s). `EXPORT_TABLE_REGISTRY` include/exclude calls match product intent — `unlock_state`/`finding_rule_links`/`sync_runs`/`coverage_gaps` describe the trader's own progress/trading, correctly included; `analytic_renders`/`user_cohorts`/`shadow_runs` are genuinely internal-engine-only (each has Module 05's own DDL/§ language quoted as the reason), correctly excluded. `rule_evaluations` selected as-stored (`result`/`reason`/`observed`/`frozen_at`), never recomputed; `export-tables.ts` imports nothing from `lib/rules` (grepped). `truncated` flag present per-table and tested, never silent.
  **Real gap, why this is FAIL not PASS-with-notes**: the CSV half of story 5.1's "JSON + CSV bundle" was never touched. `tradingAccountsToCsv` (`lib/privacy/export.ts:189`) still emits only trading-account rows; the privacy page (`app/(app)/privacy/page.tsx:232`) and the transactional email (`retrospeq-design-system/brand/templates/email/export-ready.html`) both promise "trades, fills, rules, evaluations, strategies, fields ... as JSON and CSV" — a trader downloading the CSV today gets none of that. `export.ts:184-187`'s own comment is stale ("Once Module 02 exists") though Module 02 shipped 2026-08-23 and this very commit added `trades` to the JSON registry. The `docs/infra-gaps.md` line this commit closed explicitly named "and its CSV counterpart, `tradingAccountsToCsv`" as in scope — marking it "CLOSED 2026-09-15" with zero CSV mention is a dishonest closure, not merely an incomplete feature.
  Minor, non-blocking: `tables` uses raw snake_case DB columns with no in-bundle README/shape doc for the trader — acceptable given this slice's own "interim pre-gate" framing, not itself a fail. No ADR needed for the exclusion policy (each exclusion is narrow, security/internal-bookkeeping-reasoned, matches 00-foundation's security bar, not a spec deviation).
  **Send back to coder**: reopen/qualify the infra-gaps line naming CSV specifically, extend `tradingAccountsToCsv` (or a new multi-table CSV path) to cover at least what the email/page copy promises, fix the stale Module-02 comment. Backend-only, no dev server/screenshot needed. Not committed; no files reverted.
- **2026-09-15 · SECURITY · Tier-3 review of 589807c, privacy export completeness · PASS.** Table/column/orderBy names in `export-tables.ts` are fixed literals, never from `userId`/caller input; every query is `where user_id = $1`. `userId` traced end-to-end: `requestExportAction` (actions.ts) uses `requireUser()`'s session id -> `data_requests.user_id` (owner-RLS INSERT) -> `buildExportBundle`, never client-supplied. `withServiceRoleConnection` used for future-worker callability, not to bypass narrowing — scoping stays explicit per-query. Denylist checked against every migration: no sessions/TOTP/API-key/invite/webhook table exists; `account_credentials`/`mfa_recovery_codes` are the only credential tables, both denylisted + live-tested with real seeded rows, markers never in `JSON.stringify(bundle)`. New `audit_log` inclusion checked for cross-user leakage in `metadata` (3 writers repo-wide: expiresAt/error-message/own-userId only, no secrets or other-user ids). Cross-user isolation live-tested. Service-role-inventory allowlist entry updated with a real reason. DoS: pool `max:3` bounds the 40-query `Promise.all`; per-table 50k-row bound with honest `truncated` flag; export action itself unchanged, already rate-limited + duplicate-guarded. No new tables in this diff — RLS n/a. Ran `check:security` 323/324 (1 fail: `analytic_user_suppression`, unrelated file, matches dispatch's known-flake list); `export-completeness.live.test.ts` 4/4, `export-tables.test.ts` 8/8, `export.test.ts` 7/7. Not committed.
- **2026-09-15 · CODER · `lib/privacy/export.ts` completeness (Module 01 §5.1/§8) · tier 3.** New `lib/privacy/export-tables.ts`: `EXPORT_TABLE_REGISTRY` (40 real `retrospeq` tables with a `user_id` column, generated from a live `information_schema` introspection, not guessed), each with a select column list, `plainDateColumns` (only bare `date`-typed cols need `::text` — timestamptz already fixed globally by `pg-type-parsers.ts`, confirmed live), and `orderBy`. `fetchOwnedRows` casts/orders/bounds (`EXPORT_ROW_LIMIT=50_000`, `truncated` flag, never silently drops). `EXPORT_EXCLUDED_TABLES`: `account_credentials`/`mfa_recovery_codes` (credential material) + `analytic_renders`/`analytic_user_suppression`/`shadow_runs`/`user_cohorts` (internal-analytics-engine-only, never user-facing), each with a written reason. `trading_accounts`/`subscriptions` stay on their pre-existing hand-typed fields (`EXPORT_LEGACY_TYPED_TABLES`) for backward compat, not duplicated. `export.ts`'s `buildExportBundle` now also builds `ExportBundle.tables` (concurrent `Promise.all`, pool `max:3` bounds actual concurrency).
  New `export-completeness.live.test.ts`: (1) live `information_schema` scan asserts every table with a `user_id` column is registered/legacy-typed/excluded — fails on a future silent gap, same mechanism as `service-role-inventory.test.ts`; (2) denylist — seeded a REAL `account_credentials`+`mfa_recovery_codes` row, proved the markers never appear anywhere in `JSON.stringify(bundle)`; (3) cross-user isolation — two real users, `audit_log`+`strategies` rows, proved no cross-contamination. `profiles` (PK IS the user id, no `user_id` column) documented as a 4th, structural exception. Unit: `export-tables.test.ts` (registry invariants, SQL-shape of `fetchOwnedRows`) + rewrote `export.test.ts`'s mock from call-order to query-content-matching (call order broke once 40+ queries were added).
  `docs/infra-gaps.md`/PROGRESS.md cross-cutting line closed. `npm run verify` tier 3: tsc/eslint clean; unit 110/110 (`lib/privacy`) + 21/21 (`app/(app)/privacy`); live completeness/denylist/isolation 4/4; `check:security` 324/324, import-boundaries clean, security-grep ok. Not committed. Needs security ‖ qa.
- **2026-09-15 · QA · Re-verify: plan-gated findings fix (15d4ed0 on fa25d22) · PASS.** All 3 read sites (`findings-service.ts`, `weekly-findings.ts`, `field-introduction-repository.ts`) omit `reason==='plan'` before any payload/candidate is built; every other block reason unchanged — confirmed against diff. ADR 0035 addendum is substantive (distinguishes permanent vs. transient reasons, reconciles with decision #4's original scope, covers all 3 sites). Weekly 3-slot cap can no longer be filled by plan-blocked candidates (code + passing live test). Ran the 2 unit + 4 live test files named in the CODER entry: 14 unit + 23 live tests, all pass. `check:import-boundaries` clean.
  Default-strategy screen (`strategies/[id]/page.tsx`, untouched by this diff): Pro-gated fields with a REAL computed row are now omitted (not fake "insufficient"); fields with NO representative row yet (no data source, e.g. `drv.session`/`drv.order_type`, or genuinely under-sampled) still show honest `n:0`/"more trades needed" regardless of plan — disclosed narrowly in the fix's own test comment as pre-existing, out of this fix's scope, not swept under the rug. "9 fields" header reports the roster size (`orderedFields.length`), independent of the findings list below — a distinct, accurate claim, not a re-inflated illusion.
  Session-vocabulary gap confirmed genuine: grepped `modules/*.md` for London/session — names (Tokyo/London/NY) appear as examples but no UTC boundary hours or rollover convention are defined anywhere; inventory 1.19 ◐ and infra-gaps/NEEDS_YOUR_INPUT entries are honest, not overstated either way. No dev server needed (static + test evidence sufficient). Not committed.
- **2026-09-15 · CODER · Fix QA FAIL: plan-gated findings ≠ "not enough data yet" (fa25d22 follow-up) · tier 2.** Root cause matched QA exactly: `canRender=false` was folded into `buildNoDataFindingPayload` (ADR 0035 decision #4) for EVERY block reason, including `'plan'` — a permanent wall, not a transient one. Fixed at all 3 real read sites: `findings-service.ts` (`getStrategyFieldFindings`), `weekly-findings.ts` (`assembleWeeklyFindings` — here a fake candidate could also displace a real one from the 3-slot cap), `field-introduction-repository.ts` (framing finding now `canRender`-filtered per candidate). `reason==='plan'` → omit entirely; every other reason keeps the original fallback (ADR 0035 addendum written, not reversed). No per-field Pro lock UI pattern exists anywhere (grepped) so omission is the correct default per dispatch.
  **Bigger finding, disclosed not fixed**: `analytics-registry.md` §7's only two Free-tier derived findings, `find.session`/`find.daysession`, are unreachable by ANY user today. `drv.session` has no data source (`field-values.ts`'s own flagged gap since Module 03's original migration) — live-proven zero `findings` rows ever written for it even at 50 trades. `find.session`'s own `analytic_config` is also `cohort_only=true` (beta). `find.daysession` doesn't exist in code; needs the same missing session vocabulary PLUS a composite field this single-field-only segmentation engine can't produce. Did NOT invent session UTC boundaries (a real product decision) — `NEEDS_YOUR_INPUT.md` entry added. `docs/infra-gaps.md` updated (1 closed, 1 new). Inventory 1.19 ●→◐ (mechanically correct now, but zero real path for an ordinary free trader given the vocab gap).
  Tests: 2 unit files updated (`findings-service.test.ts` +2, `field-introduction-repository.test.ts` +2) proving plan-blocked≠insufficient and multi-candidate fallthrough; 4 live files updated/extended (`default-strategy-edge-integration.live.test.ts`, `findings-service.live.test.ts`, `field-introduction-repository.live.test.ts`, `weekly-findings.live.test.ts`) — all run directly, all pass. tsc/eslint clean on every touched file. Did not run full `npm run verify` sweep (narrow live runs sufficed given the known repo-wide flakes named in the dispatch); not committed per dispatch instruction.
- **2026-09-15 · GATE · Module 08 §5.4/§5.5 default-strategy reachability fix (fa25d22) · QA FAIL (product-intent, not a one-liner).** Verified: `origin<>'captured'` keeps "zero CAPTURED fields" literally, no prompt/UI push added (silent) — PASS. Thresholds untouched (no `edge-engine/` files in diff); ran `default-strategy-edge-integration.live.test.ts` myself, 2/2 pass, 11 trades → all `insufficient`/`null_result`. No version-history UI exists (`strategy_versions.version` never rendered — grepped). `check:import-boundaries` clean; no Home/dashboard surface touched.
  **Real finding**: `resolveAnalyticId` (`edge-engine.ts:86`) only carves out `drv.session`→`find.session` (`min_plan='free'`, migration `20260911010000`); the other 8 seeded derived fields fall through to the SAME ids captured judgment fields use — `find.pickone`/`find.toggle`/`find.number`, all `min_plan='pro'`. Default plan is `'free'`. So 8/9 seeded fields can NEVER pass `canRender` for a free user at any trade count — permanently reads as "not enough data yet" (`findings-service.ts:120`), contradicting design-decisions §15 ("Free: ... derived findings") and §6 Stage-1. Screenshot-confirmed (`strategies/[id]/page.tsx`, throwaway user): "9 fields" header + 9 cards, indistinguishable from a trader-configured strategy. Security's PASS cited `capability-table.ts`'s `analytics.derived`, which the real render path never consults (uses `analytic_config.min_plan` instead) — not evidence against this.
  Needs an origin-aware analytic-id/config decision (extend the session carve-out, or a new free "derived" id family), not a one-line patch — send back to coder; soften inventory 1.19 / infra-gaps' "CLOSED" line to name the plan-gate gap. Cleaned up: qa user deleted, dev server (started for this check) stopped, temp files removed.

- **2026-09-15 · SECURITY · Module 08 §5.4 default-strategy seed backfill (fa25d22) · PASS.** `check:security` 324/324 (flake from prior run not reproduced) + all 8 new/touched live tests (`fields-repository.default-strategy-seed`, `default-strategy`, `default-strategy-edge-integration`) pass directly. Migration guard verified live: exactly the pre-existing empty-fields default strategy backfilled to v2 (9 fields), 3 others already created fresh at v1 by the new code path — 0 empty-v1-default rows remain, idempotency confirmed. Fields subquery scopes `f.user_id = r.user_id` (no cross-user field id can enter another user's `fields[]`/`field_usages`, live-tested). `strategy_versions_forbid_mutation` respected: migration's only UPDATE sets `superseded_at` null→now() (the one permitted mutation); v2 is a fresh INSERT, not a mutation. `fetchDefaultStrategySeedFieldIds` uses `withUserConnection` (not service role) — no allowlist entry needed, confirmed via inventory test. Entitlements: `analytics.derived` is `{free:true,pro:true}` — seeding derived fields into a free default strategy grants no Pro-gated capability; `createStrategy's` `isDefaultStrategy` entitlement bypass is pre-existing (unchanged by this diff), and the raw-SQL backfill bypassing `editStrategy's` Pro check is correctly scoped (system catch-up, not a user-editable path — SQL never reachable by any client). Minor note, not a fail: migration's UPDATE/INSERT pair has no rowCount check like `applyStrategyEditVersion's` optimistic lock — a concurrent live `editStrategy` racing the same row would abort the migration txn via the unique-index conflict (fails loud, no corruption); acceptable for a one-time backfill on a low-traffic shared dev DB. No vendor types, rule engine, or Server Actions touched.
- **2026-09-15 · CODER · Module 08 §5.4/§5.5 default-strategy reachability gap · tier 3.** `fetchDefaultStrategySeedFieldIds` (new, `lib/fields/fields-repository.ts`): `kind <> 'strategy_var' and origin <> 'captured'` — today, exactly the 9 permanent `drv.*` rows (always seeded at signup, before any sync/connect); keeps §5.4's "zero CAPTURED fields" promise since a trader-typed `account` field always has `origin='captured'` (`createField`). `ensureDefaultStrategyForUser` now seeds new default strategies with this list (`captureMoment:'post_close'`, inert — edge engine reads only `field_id`). Migration `20260915030000_default_strategy_seed_fields_backfill.sql` (applied to shared dev, verified, idempotent) backfills the one existing empty-fields default strategy via supersede-then-insert-v2 done directly in SQL, NOT via `editStrategy` (which requires Pro `strategy.create` entitlement — would wrongly reject every free-tier user). Fixed two pre-existing tests whose `fields = []` assertions this change correctly breaks (`default-strategy.live.test.ts`, `sync.live.test.ts`'s Slice 08b test).
  Live-proven end-to-end (`default-strategy-edge-integration.live.test.ts`): 50 confirmed trades → real `confident`/`provisional` finding on `drv.direction`; 11 trades → segments computed but all `insufficient`/`null_result`, never shown — no threshold lowered. Selection-logic live tests (`fields-repository.default-strategy-seed.live.test.ts`): derived in, `account`/`strategy_var` out, cross-user isolation.
  Verify: tsc/eslint/import-boundaries clean; unit 208/208 touched-dir; new/touched live tests all pass when run directly; `check:security` 323/324 (1 pre-existing unrelated `analytic_config` PK flake, untouched file, matches this week's documented pattern) — `npm run verify`'s own `vitest related` sweep (huge transitive graph off `fields-repository.ts`) hit two ALREADY-KNOWN pre-existing flakes (`decisions-relaxation-integration.live` CROSS-KIND, `sync.live.test.ts` stale-trade-backlog timeouts) plus the since-fixed Slice-08b assertion — re-ran narrowly to confirm, documented here rather than re-run in full (60+min historically for this scope). `docs/infra-gaps.md` entry closed; inventory 1.19 ○/◐→●. Needs security ‖ qa.
- **2026-09-15 · GATE · Module 08 §5.5 field-introduction offer (frame 1.19) · QA PASS.** Checked c0701f5 against 08 §5.5/§6 and design-decisions §8.2. Conditions exact and unit+live tested (`field-introduction.ts`, 13/13; `field-introduction-repository.live.test.ts`, 4/4 — real finding shown/excluded, cooldown stamp, decline count). "Shown" proxy (`analytic_renders` same-`analytic_id` correlation, no FK) is the honest narrowest option available, documented not silently approximated. Framing statement reuses Module 05's own audited `buildFindingPayloadFromRow` (ADR 0035) — observation only, never invented. Home: rendered (static HTML + real component via `renderToStaticMarkup` + brand CSS + Playwright, light/dark, no dev server) — matches frame 1.19 (`brand/docs/screens/home-onboarding.html#1.19`) layout/marks/copy; Clear branch has no other `.rq-btn` (grepped); no currency/red-green; `.rq-num` correctly omitted on the prose statement per existing 0035 precedent. `/fields/new` reachable outside the offer: confirmed via Rulebook tab → `RulebookSubnav` "Fields" pill → `/fields` → "Add a field" `.rq-btn` (`app/(app)/AppShellNav.tsx:108-110`, `FieldsList.tsx:247`) — real path exists. `check:import-boundaries` clean; runbook entry substantive; inventory row honestly ◐.
  **Product judgment call**: the disclosed unreachability (default strategy has 0 fields; edge engine only computes over a strategy's own field list, `edge-engine/repository.ts:76`) is a **pre-existing gap, faithfully disclosed — PASS, not a slice failure**. §5.5's own logic is correctly built and live-proven against a strategy that *does* have a field; the gap is upstream (§5.4/Module 05 integration), not introduced or hidden by this slice, and is already tracked (`docs/infra-gaps.md`, PROGRESS Next-up #2). Narrowest fix: seed `ensureDefaultStrategyForUser`'s created version's `fields` (`lib/onboarding/default-strategy.ts:63`, currently `[]`) with the user's derived/account field ids at creation — adds no captured field, so §5.4's "zero captured fields" holds. No fixes needed; slice closeable once security's PASS lands.
- **2026-09-15 · GATE · Module 08 §5.5 field-introduction offer (frame 1.19) · SECURITY PASS.** Checked c0701f5 (no migration). `actions.ts:83-111`: zero client input by design (matches `closeWeeklyReview` precedent), session-derived user only, `requireSessionAndRateLimit('fieldIntroductionOffer')` on both actions, redirect target hardcoded `/fields/new` (no open redirect), accept relies on that route's own existing entitlement gate — correct, not a bypass. `field-introduction-repository.ts:128-174`: `withUserConnection` (RLS, not service-role — no new service-role site, allowlist test 3/3 unaffected), every joined table explicit `user_id=$1` (defense in depth), `fl.kind <> 'strategy_var'` correctly excludes captured fields from framing (live-tested). `onboarding-state-repository.ts`: `recordFieldsDeclined` atomic `+1` UPDATE (race-safe); `recordFieldsOffered` sets `now()` only, no counter to race; no path resets either field. GET-time stamp from `page.tsx`'s Server Component: confirmed via `node_modules/next/dist/docs` — `/dashboard` has no `loading.tsx` and is fully dynamic (reads cookies), so Next.js *skips* prefetching it entirely (dynamic-route-without-loading.tsx rule) — Link prefetch from the nav/review pages never triggers the write; a real visit's write is idempotent (timestamp overwrite) regardless. No rate-limit-bypass reader added. No credential material logged (only `userId` in `console.error`). `npm run check:security` 323/324 (pre-existing `analytic_user_suppression` flake only, matches ledger). New tests run directly: `field-introduction.test.ts` 13/13, `field-introduction-repository.test.ts` 8/8, `field-introduction-repository.live.test.ts` 4/4, `service-role-inventory.test.ts` 3/3. No fixes needed.
- **2026-09-15 · CODER · Module 08 §5.5 field-introduction offer (frame 1.19) · tier 3.** No migration — schema (`onboarding_state.fields_offered_at`/`fields_declined_count`) pre-existed 08a. New: `lib/onboarding/field-introduction.ts` (pure eligibility, 30 trades/30-day cooldown/2 declines/already-introduced), `field-introduction-repository.ts` (real derived-finding read: `findings` joined to `fields` excluding `kind='strategy_var'`, correlated to a real `analytic_renders` row as the "shown" proxy — logged since no FK links the two), two new `onboarding-state-repository.ts` writes (`recordFieldsOffered`/`recordFieldsDeclined`, deliberately stage-blind), `app/(app)/dashboard/{actions,FieldIntroductionOffer}.tsx` (accept -> `/fields/new`, decline -> free+recorded), new `fieldIntroductionOffer` rate-limit scope.
  **Reconciliation, found not fixed**: the silent default strategy (§5.4) has 0 fields, and the edge engine only computes findings for a strategy's OWN chosen field list (`fetchStrategyFieldSpecs`) — so no derived finding is EVER computed for a stock default strategy. The offer's "≥1 derived finding shown" condition is honestly correct (never invents one) but is unreachable for a typical trader until this closes (`docs/infra-gaps.md`, new entry). Also decided: the offer is NOT sticky across renders — stamped once per episode, disappears until the 30-day cooldown or a decline, matching frame 1.18's "quiet, once" precedent (§5.5 doesn't specify persistence).
  Verify: tsc/eslint clean; unit 39/39 new + 82/83 touched-dir (1 pre-existing skip); live 4/4 new (cooldown stamp, strategy_var exclusion, state writes) — ran directly since verify's `vitest related` swept `lib/rate-limit/config.ts`'s huge import graph into unrelated pre-existing flakes (`sync.live.test.ts` timeouts, `decisions-relaxation-integration` CROSS-KIND, both already logged this week); `check:security` 323/324 (1 pre-existing `analytic_user_suppression` flake); `check:import-boundaries` clean. Render-checked (static HTML + brand CSS + Playwright, no dev server) against frame 1.19 — matches. Accept routes to the existing Pro-gated `/fields/new` (not re-checked here; that route already gates itself). Inventory 1.19 ○→◐, honestly caveated. Needs security ‖ qa.
- **2026-09-15 · GATE · Module 06 §4.10 step 6 / Module 07 §5.6: the one weekly notification · QA PASS.** Checked 6c70509..47b8635. Exactly-once: unique `(user_id,period_start)` + atomic claim-before-send, live-proven. Repo-wide grep: only `erasure.ts`/`email-provider.ts`/`weekly-job.ts`/`review-notifications-repository.ts` touch Resend; `no-notifications.test.ts` statically forbids `lib/engagement/` from ever importing it. Copy vs `brand/templates/email/weekly-review-ready.html`: rendered via `npx tsx` + Playwright screenshot, pixel-identical to the template. No currency/streak/anxiety wording, no red/green, honest zeros for real counts (not a derived stat, so not a "not enough data" case). No adherence numbers in the email, so no hard/soft blending risk. `/privacy` opt-out: static review — matches pre-existing `TelemetrySection` toggle pattern exactly (one ghost button, no primary); page's multiple ghost buttons across sections predate this diff. `run-weekly-review-job.mjs` genuinely refuses (exit 1, no fake success). Runbook alerting entry substantive. Inventory 4.14 ○→◐ honest (push half not built, new infra-gap entry added). Ledger status lines match. **Pointable, not blocking**: "no auto-retry on failed send" is a real reliability decision, documented only in code/runbook/ledger, no `docs/adr/` file like sibling 0043/0044 — worth one if retry behaviour is revisited. No fixes needed.
