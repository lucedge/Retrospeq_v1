# Retrospeq build ledger

The one file an agent reads cold to know where the build is. **Hard cap: 200 lines** (`scripts/ledger-check.mjs`, runs in the pre-commit hook). History lives in `docs/ledger/` (append-only, grep it, never read it in full). Standing infra gaps live in `docs/infra-gaps.md`. Things blocking work *now* live in `NEEDS_YOUR_INPUT.md`.

## Autonomy policy

Owner-approved 2026-08-19: agents may commit and push to `main` with no human review gate. This does not waive the spec's own quality bar (AGENTS.md "Security bar" / "Testing bar") — a module is not "done" until those pass. Cost/cadence is the owner's call: local `/loop` only, the cloud routine (`trig_01NV6fHZShY1bPQindEH7dc2`) stays paused, never auto-resume, no "usage exhaustion detection" (owner decision 2026-08-20).

## Phase status

| Phase | Scope | Status (2026-09-14) |
|---|---|---|
| 0 | Golden fixtures + shadow harness | **Done.** 8/8 fixtures (`fixtures/golden/`), harness infra + `shadow_runs`. No real shadow analytics registered yet. |
| 1 | Module 01 Identity · Module 02 Ingestion | **Done 2026-08-23.** |
| 2 | Module 04 Rulebook · Module 08 Onboarding | **04: done for reachable scope** (slices 1–10f). Left: 10c discovery (needs 05 wiring), strategy-scoped rules 1.5–1.7 (blocked on the operand-catalogue product decision in `NEEDS_YOUR_INPUT.md`). **08: partial** — 08a schema, 08b onboarding router + honest-fallback Hook, degraded dashboard (Clear / Trades-to-close + minimal open-position). Left: calibration screen (three seeded rules, §5), silent default strategy (§5.4), full four-state home (position-open card, review-ready), real T0 hook. |
| 3 | Module 03 Fields & Strategy · Module 05 Analytics | **03: done** (backend closed 2026-09-09, all six §5.1 UI elements). Known gap: `strategy_var` owner-scoping (`docs/infra-gaps.md`). **05: backend done** — edge engine, detection engine + v1 catalogue, `rule_proposable`, §4.6 improvement, decay, asset-class suppression, weekday canary. No UI surface of its own (findings render via 03/06). |
| 4 | Module 06 Review · Module 07 Engagement | **06: slices 1–7 done** (schema, close-out completion, read payload, candidates, ranking/cap, Part 1 UI `/review`, Part 2 graduation + relaxation at `/review/decisions`). Left: promotion / retirement / detection decision flows, Part 3 close, deferral backlog, monthly trend, §4.8 four-week expiry, adherence-timeline annotation render, scheduler (infra). **07: streak only** (slice 1). Left: `engagement_events`, XP, milestones, the one weekly notification. |
| UI | App shell + visual build | **Shell done (interim, 2026-09-13)**: four tabs, phone-width column, `/settings`, `/performance` placeholder. tester + security PASS, qa stopped by owner. **Next: owner-directed design-system build (owner supplies the process), then remaining features, then screens against the mockup.** `brand/` is visual authority over `modules/09-design-system.md`. |
| v1.1 | Module 09 Prop firms · Module 10 AI | Deferred. |

Cross-cutting follow-ups (details in `docs/infra-gaps.md`): `lib/privacy/export.ts` omits trades/rules (data-rights, owner to prioritise) · repo-wide Zod `.strict()` sweep · RLS FK-ownership sweep · shared dev project's stale-trade backlog slows `autoConfirmStaleTrades` live tests.

## Current task

**2026-09-14 — Module 06 promotion decision flow** (frame 4.8; dispatching). Just closed: Module 06 Part 3 close the week (QA PASS; security FAIL on a check-then-update race → both writers now lock the review row → security re-verify PASS with a genuine two-connection test). Earlier today: Module 08 silent default strategy, four-state Home, calibration restyle. Dev server kept off unless a slice needs it (owner request). Owner to fill `TODO(owner)`: Pro price, domain.

## Next up

1. Module 06: promotion / retirement / detection decision flows (frames 4.8–4.10) → deferred backlog + 4-week expiry (4.11) → monthly trend (4.13) → adherence-timeline annotation render.
2. Module 07: `engagement_events` + XP (never for adherence) → milestones (inline, frame 1.18) → the one weekly notification (build against interface; scheduler + email provider are infra gaps).
3. Module 08: field-introduction offer (§5.5, frame 1.19).
4. `lib/privacy/export.ts` completeness; Module 04 discovery (10c, frame 3.10).
5. Repo-wide sweeps: Server Action `.strict()`, RLS FK ownership, `strategy_var` owner scoping.
6. Phase end: full suites, `/code-review`, `retrospeq-docs`. Then UI phase: restyle remaining ◐ rows in `brand/docs/inventory.md` (tier 1).

## Decision log

Newest first. **One entry ≤ 20 lines**, template in `.claude/skills/ledger/SKILL.md`. When this section passes ~40 entries, move the oldest to `docs/ledger/decision-log-<from>_to_<to>.md`.
- **2026-09-14 · GATE · Transactional email wired to Resend (`lib/privacy/email-provider.ts`) · SECURITY PASS.** Owner-requested side slice (interactive session, parallel to Module 06). Owner set up Resend: verified domain `notifications.retrospeq.com`, custom SMTP on the shared Supabase project (Auth emails previously 500'd, `NEEDS_YOUR_INPUT` entry cleared, auth E2E 5/5), `RESEND_API_KEY`/`EMAIL_FROM` in `.env.local`. Provider: plain `fetch`, no SDK; `server-only`; missing/blank env throws `EmailProviderNotConfiguredError` naming the var; `EmailSendFailedError` carries status + Resend name/message only, never key/recipient/body; CR/LF + recipient + length validation; 10s timeout; no retries (erasure is best-effort, avoid double-send). check:security 301 pass; lib/privacy 96/96; eslint + security-grep clean. Live: two owner-requested test sends via the real module accepted. Gates: coder + security only (library adapter, no product surface, real sends stronger than a tester pass). E2E emails now `delivered+retrospeq-e2e-…@resend.dev`. Remaining: env vars on the deployment once a Vercel project exists.
- **2026-09-14 · GATE · Module 06 Part 3 close the week · SECURITY RE-VERIFY PASS.** Checked HEAD (1b2f3ff) against the FAIL. `markReviewCompleted` and `writeReviewPrompts` both take `select ... for update` on the same `reviews` row first (`reviews-repository.ts:275`, `review-prompts-repository.ts:80`), so the two serialise; `markReviewCompleted` then does one atomic conditional `UPDATE ... WHERE NOT EXISTS(pending)` under the lock, `writeReviewPrompts` returns `[]` if `completed_at` is already set. Grepped all `review_prompts` writers — the only other inserter is this same `writeReviewPrompts`; `decisions/prompts-repository.ts`'s writes only transition pending→accepted/deferred, never insert new pending rows. New live test genuinely blocks (`waitForBlockedQuery` polls `pg_stat_activity` for `wait_event_type='Lock'` before releasing) and asserts refusal. `npx vitest run mark-review-completed.live.test.ts review-prompts.live.test.ts` 17/17 pass. No fixes needed.
- **2026-09-14 · GATE · Module 06 Part 3 close the week · SECURITY FAIL (1 finding, fixable).** Checked 84b4796..9a0e068 + `check:security` (300/301 live pass; 1 fail is a pre-existing 5s timeout in `analytics-registry-schema.independent-verify.rls.test.ts`, Module 05, file untouched by this diff — unrelated). `closeWeeklyReview` (`app/(app)/review/actions.ts:699`) takes no client input, period server-derived, behind `requireSessionAndRateLimit('weeklyReview')`; `WeeklyReviewBody.tsx`'s `useActionState` wrapper passes nothing through — PASS. `fetchLatestCompletedWeeklyReviewId`/`fetchDecidedPromptOutcomes` — `withUserConnection`, `user_id=$1` scoped — PASS. `upsertWeeklyReview`'s `ON CONFLICT DO UPDATE` still omits `completed_at`/`opened_at` from its SET list — completed reviews stay frozen — PASS. No new `withServiceRoleConnection` sites; no credential material logged. **FAIL**: `markReviewCompleted` (`lib/review/reviews-repository.ts:252-284`) enforces the pending-prompt refusal as a separate `SELECT ... has_pending` then `UPDATE`, not one atomic conditional statement, and takes no row lock — `writeReviewPrompts` (a *different* transaction, `withServiceRoleConnection`, runs on first-ever page materialization) can INSERT a fresh `pending` row between the check and the commit (reachable via a genuine double-tab/duplicate-request race on first `/review` load, not purely theoretical). Fix: single `UPDATE ... WHERE NOT EXISTS (SELECT 1 FROM review_prompts WHERE review_id=r.id AND state='pending')` (the atomic-conditional-UPDATE pattern AGENTS.md requires for this exact class of race).


- **2026-09-14 · GATE · Module 06 Part 3 close the week · QA PASS (2 pointable, disclosed).** Checked 84b4796..9a0e068. `closeWeeklyReview` (`lib/review/reviews-repository.ts:266-304`) refuses with `pending_prompts` unless every prompt left `pending`; `renderWeekCloseSummary` (`format.ts:62-95`) builds only from `fetchDecidedPromptOutcomes`, "Nothing changed." when none — never invented; exact spec copy "Next review Sunday. Nothing to do until then." (`WeeklyReviewBody.tsx:75`, `page.tsx:196`); one `.rq-btn` per state (close view: single ghost "Back to home"; Part 1: one button whether decisions-pending or zero-prompt); zero-prompt week closes in one tap, no confirmation step; revisiting a closed period renders the closed view via `completedAt!==null` (`page.tsx:169`), never falls back to Part 1; `computeReviewReadyState`'s period-cursor advance (unchanged this diff) means Home stops showing review-ready once closed. Inventory 4.2/4.12 honestly disclose known gaps, not silently marked done. `vitest run` format.test.ts+page.test.ts 16/16; `check:import-boundaries` clean; no currency/red-green in diff (grepped). **Pointable, not blocking**: (1) close view omits mockup 4.12's streak strip — already disclosed in inventory, not hidden; (2) `renderWeekCloseSummary`'s multi-rule clause emits a raw digit with no `.rq-num` wrap (plain string return, rare path — most weeks are "Nothing changed."). No ADR needed — extends ADR 0039, no new deviation.

- **2026-09-14 · CODER (finished inline) · Module 06 Part 3 close · tier 3.** Coder agent stalled at its E2E run; orchestrator finished verification. Files: `lib/review/reviews-repository.ts` (`markReviewCompleted` — coalesce, owner-scoped, refuses with pending prompts; `fetchLatestCompletedWeeklyReviewId`), `lib/review/review-prompts-repository.ts` (`fetchDecidedPromptOutcomes`), `app/(app)/review/actions.ts` (`closeWeeklyReview`, no client input, rate-limited; `caught_up` now carries the last close summary), `page.tsx` + new `WeeklyReviewBody.tsx` (enabled "Week closed" submit, frame 4.12), `format.ts` (`renderWeekCloseSummary`, never invented). Bug found and fixed: after closing, revalidation fell through to a generic "You're caught up." and lost the closed view — `caught_up` now renders frame 4.12 from the latest completed review. Verify: tsc clean; review unit tests 10/10 + 94/95 (1 relaxation live test failed only under parallel load, 10/10 alone); new live `mark-review-completed` passes; `e2e/review-weekly-read.spec.ts` 3/3 after a dev-server restart (DB `ETIMEDOUT` crashed the first server). Looked at: close-after-submit screenshot. Needs security ‖ qa.
- **2026-09-14 · GATE · Module 08 §7 full four-state home · QA PASS.** Re-checked 3fd761f..HEAD + uncommitted fix. Confirmed the prior QA-FAIL bug is fixed (working tree, uncommitted): `computeReviewReadyState` now returns `adherence: null` on `insufficient_history` (`dashboard-repository.ts:264-271`) and `ReviewAdherenceCmp` renders "Not enough data yet" for that case (`page.tsx:212-221`), never "0 of 0"; unit test updated to match, `vitest run lib/dashboard` 25/25. Verified: one state/one action (0 or 1 `.rq-btn` per state, closeout+review only); no currency (grep clean); hard/soft never blended anywhere (Clear screenshot shows two separate rows, 9/10 hard + 4/4 soft); streak in weeks; inventory rows 1.12/1.13/1.15/1.16 correctly ● and match frames (pulled via `sed`); `.rq-num` on all numeric spans; `check:import-boundaries` clean (analytics still doesn't import rules); `tsc`/`security-grep` clean. **Caveat**: `tmp/dev-screenshots/dashboard-review-ready.png` is stale (pre-fix, still shows the old "Soft · 0 of 0" bug) — didn't re-render given time budget; relied on code + updated unit test instead, both correct. **Pointable, not a decision**: the fix is real but uncommitted — coder to commit before this closes.

- **2026-09-14 · GATE · Module 08 §7 review-ready adherence fix · QA RE-VERIFY PASS.** Checked HEAD (8224929) against the prior FAIL. `computeReviewReadyState` (`lib/dashboard/dashboard-repository.ts:264-268`) now sets `adherence: null` on `insufficient_history` instead of `{0,0}`; `ReviewAdherenceCmp` (`app/(app)/dashboard/page.tsx:213-221`) renders "Not enough data yet." on null or all-zero totals, gates the soft row on `soft.total > 0` (`page.tsx:242`), and the "up from" claim is gone — replaced with neutral "last week X of Y" (no forced-up direction). `npx vitest run lib/dashboard` 25/25 incl. live. Inventory 1.15 unchanged — nothing there was inaccurate. No fixes needed.
- **2026-09-14 · GATE · Module 08 §7 full four-state home · QA FAIL (pointable).** Checked 09a668b..HEAD. PASS: ranking open>closeout>review>clear (`dashboard-state.ts:38`); one `.rq-btn` per actionable state; no currency; Clear's hard/soft never blended, e2e-verified live (9/10 hard + 4/4 soft, two `.rq-dots` rows, no "13 of 14"); review-ready teaser compares soft-to-soft only; streak counts weeks; gauge only with real cap, one query, no N+1; rendered Clear (populated) matches frame 1.12. **FAIL**: `computeReviewReadyState` (`lib/dashboard/dashboard-repository.ts:257-263`) collapses `fetchPeriodAdherence`'s `insufficient_history` status into `hard/soft {0,0}` and `ReviewAdherenceCmp` (`app/(app)/dashboard/page.tsx:156-163`) renders "Soft · 0 of 0" unconditionally (no `total>0` gate, unlike hard's) — a fabricated zero where Clear's own adherence block correctly renders "Not enough data yet." Reachable when `recomputeAdherenceWeeklyForConfirmations`'s best-effort post-commit write (`confirm.ts`) hasn't landed for the period. Fix: propagate the status (or gate soft the same way hard is) instead of collapsing to zero fractions.

- **2026-09-14 · GATE · Module 08 §7 four-state Home · tier 3 · TESTER PASS.** Scope: 3fd761f..105eda3. (1) `resolveDashboardKind`: all 8 combos + fast-check property, exhaustive open>closeout>review>clear. (2) `markReviewOpened`: `withUserConnection`, `coalesce(opened_at, now())`, cross-user RLS test present + `reviews_owner` policy confirmed genuine `for all ... using(user_id=auth.uid())`; call site's `markOpenedBestEffort` catches internally, never breaks `/review`. (3) `computeReviewReadyState` covers ready/already-opened/zero-trades/insufficient_history (dashboard-repository.test.ts:237,268,289,312); uses last-ENDED ISO week (`current-period.ts`). Grepped: no blended hard+soft figure anywhere in dashboard-repository.ts/page.tsx. (4) Risk gauge only when `riskCapPct` non-null; no "Now"/live-R row; no `$`/currency on `/dashboard` (grepped). Ran: `vitest run lib/dashboard` 25/25; mark-review-opened live 4/4 (isolated); `playwright dashboard.spec.ts` 4/4. Seeded+screenshotted review-ready (390×844, `tmp/dev-screenshots/dashboard-review-ready.png`): single `.rq-btn`, hard row omitted correctly, no currency; user deleted after. **PASS. Security-reviewer: `markReviewOpened`'s write posture + `reviews_owner`'s "for all" (not SELECT-only) RLS shape — first real client write to `opened_at`.**

- **2026-09-14 · GATE · Module 08 §7 full four-state home · SECURITY PASS.** Reviewed 3fd761f..HEAD. (1) `markReviewOpened` (`lib/review/reviews-repository.ts`): `withUserConnection` (real RLS, `SET LOCAL ROLE authenticated`), `coalesce(opened_at, now())` genuinely non-destructive, `userId`/`periodStart` both session/server-derived (`requireSessionAndRateLimit` + `determineCurrentWeeklyReviewPeriod`) — no client argument surface at all in `fetchWeeklyReviewRead`; call site already behind existing auth+`weeklyReview` rate limit. (2) `computeReviewReadyState`/`fetchRiskCapPct`/`fetchRecentWeekCompletenessForUser` — every query traced to `withUserConnection(userId, ...)` with `userId` from `supabase.auth.getUser()` in `dashboard/page.tsx`; no new `withServiceRoleConnection` call sites in the diff, `service-role-inventory.test.ts` unaffected (3/3 pass). (3) No currency on Home — grep clean, only `%`/day-count strings. `check:security` 18/18 files, 301/314 clean, no boundary violations, security-grep clean; targeted live run (`service-role-inventory.test.ts` + new `mark-review-opened.live.test.ts`, incl. cross-user isolation) 7/7 pass. No fixes needed.
- **2026-09-14 · Module 08 §7 full four-state home · tier 3, coder done, security/qa PENDING.** Files: `lib/dashboard/dashboard-state.ts`/`dashboard-repository.ts` (4th `'review'` kind; `computeReviewReadyState` reuses Module 06's `current-period.ts`/`reviews-repository.ts`/`period-consistency.ts`/`period-adherence.ts`/`review-prompts-repository.ts`, no new logic); `lib/review/reviews-repository.ts` (`markReviewOpened`, first real `opened_at` write, `coalesce`-idempotent, `withUserConnection`); `app/(app)/review/actions.ts` (calls it, best-effort); `lib/engagement/week-completeness-repository.ts` (`fetchRecentWeekCompletenessForUser`); `app/(app)/dashboard/page.tsx` (all 4 states, risk-cap gauge via `fetchActiveGlobalRuleVersionsForOperand('risk_pct')`). **Reconciliation**: an initial combined hard+soft adherence count on Home (copied from mockup frames 1.12/1.15) was reverted before gates — no summary-screen exemption to "never blended" (design-decisions §6); Home now shows hard and soft as two separate counts, like `/rulebook`/`/review`. **Scope gap, flagged**: conviction dots still omitted — trades carry no `strategy_id`, no fixed "conviction" field identity to resolve generically; inventory marked accordingly. Tests: unit 18/18, live 21/21 targeted incl. new `mark-review-opened.live.test.ts` (idempotency, cross-user isolation), E2E `dashboard.spec.ts` 4/4 (Clear assertions inverted for real dots/streak). `verify` exit 0 (one pre-existing unrelated-file timeout, `freeze-evaluations.live.test.ts`, documented shared-DB flakiness class). Screenshots: `dashboard-{clear,open}-{light,dark}.png`, 390×844, throwaway users deleted. **Reviewer: `markReviewOpened` + its call site in `review/actions.ts`.**

- **2026-09-14 · GATE · Module 08 §5.4 silent default strategy · SECURITY PASS.** Reviewed f9a0751..HEAD. (1) `insertStrategyAndVersion`'s new WHERE branch (`$3=true and not exists(select 1 from strategies s2 where s2.user_id=$1)`) runs inside the existing per-user `pg_advisory_xact_lock` — genuinely race-safe, live two-connection proof present and passing; closes `docs/infra-gaps.md`'s gap as claimed, no bypass for a user with existing (any-state) strategies. (2) Both call sites — `connectManualAccount` (`userId` from `supabase.auth.getUser()`) and `runSync` (`account.user_id`/`account.platform` from the trusted account row) — are server-trusted only; no Server Action/route exposes `isDefaultStrategy` to a client; `createStrategy`'s public signature keeps it optional/internal. (3) Entitlement bypass (`canForUser` skipped entirely when `isDefaultStrategy`) is Free-tier-by-design per §5.4 and cannot mint a second row — enforced by the same `not exists` check, not just the pre-existing unique index. (4) Failure logging (`console.error` in both call sites and in `ensureDefaultStrategyForUser`) logs only userId/platform/err, no credential material. (5) No new `withServiceRoleConnection` call sites in this diff; `service-role-inventory.test.ts` unaffected, 3/3 pass. `check:security` 18/18 files, 301/314 (13 skipped) clean; import-boundaries clean; security-grep clean. No fixes needed.
- **2026-09-14 · GATE · Module 08 §5.4 silent default strategy · QA PASS.** Product intent checked against f9a0751..HEAD: (1) one strategy, name from `defaultStrategyNameForPlatform` (platform column only, never trade content), `fields: []`/`triggers: []` — zero clustering. (2) `manual` -> `'Trading'` reconciliation logged in-code (`platform-defaults.ts`) + prior gate entry, reasoning sound. (3) No prompt/notification added — both call sites are silent `try/catch`/`.catch()`, console.error only. (4) `/strategies/page.tsx` (untouched) already renders `s.isDefault && "Your default strategy."`, derived-only, honest. (5) `docs/runbook.md` entry substantive; `docs/infra-gaps.md` closed accurately; ledger's phase-status table correctly left unbumped pending this gate. No fixes needed.
- **2026-09-14 · GATE · Module 08 §5.4 silent default strategy · TESTER PASS, one test added.** Verified against commit f9a0751: (1) `insertStrategyAndVersion`'s `not exists(any strategy)` guard runs inside the per-user `pg_advisory_xact_lock`, correct. (2) Targeted run isolated (concurrent live-DB runs caused transient contention first pass — reran alone, clean): `strategy-repository.live.test.ts` 24/24, `default-strategy.live.test.ts` 4/4, `platform-defaults.test.ts` 6/6, `actions.test.ts` 36/36 — 70/70. (3) The "any state" archived-strategy case and a genuine two-real-connection race proof (`waitForBlockedQuery` on `pg_stat_activity`, not sequential) both exist and pass. (4) Gap found: `connectManualAccount` had a forced-failure non-blocking unit test; `sync.ts`'s wiring did not. Added one to `lib/ingestion/__tests__/sync.live.test.ts` (`vi.spyOn` the real module, no `resetModules`/`doMock` — that approach hung this file's DB-cleanup hook under load) — passes, proves a rejected `ensureDefaultStrategyForUser` never fails `runSync`. (5) `check:security` 18/18 files, 301/314 (13 skipped) clean in isolation; one `analytic_user_suppression` permission-denied failure seen only under concurrent load, unrelated file, reran alone clean — environmental. File touched but **not committed**: `lib/ingestion/__tests__/sync.live.test.ts`.
- **2026-09-14 · GATE · Module 08 §5.4 silent default strategy · tier 3, coder done, security-reviewer/qa PENDING.** Files: `lib/fields/strategy-repository.ts` (`insertStrategyAndVersion`'s guarded INSERT now requires `not exists (any strategy)` inside its existing per-user advisory lock for `isDefaultStrategy=true`; new `DefaultStrategyAlreadyExistsError`), `lib/broker/platform-defaults.ts` (`defaultStrategyNameForPlatform`), new `lib/onboarding/default-strategy.ts` (`ensureDefaultStrategyForUser`), wired at both `history_imported` sites (`lib/ingestion/sync.ts`, `app/(app)/accounts/actions.ts`). Closes `docs/infra-gaps.md`'s `isDefaultStrategy` gap, marked `[x]`. Reconciliation (logged in-code): `manual` platform → `'Trading'`. Tests: 28/28 live incl. non-default/archived/genuine 2-connection race proofs + new `default-strategy.live.test.ts`; 292/292 unit incl. wiring assertions. `verify` tier 3: tsc/eslint clean; live run hit 3 PRE-EXISTING `sync.live.test.ts` timeouts (unrelated tests, confirmed environmental by isolated rerun — same 3 fail alone, no per-test override, ~120 lines from my edits). `check:security` run separately (verify stops at first fail): 18/301 PASS, boundaries + grep clean. **Reviewer: `insertStrategyAndVersion`'s new WHERE branch + the advisory-lock race proof.**

- **2026-09-14 · GATE · `/rules/start` mockup fidelity (Module 08 §5.3) · PASS.** Scope: `app/(app)/rules/start/{GuidedFrontDoor.tsx,page.tsx}`, tier 1. Rebuilt each card as `li.calibrate__rule.rule-editor` inside `ol.calibrate__list`: sentence split around `{value}` (new `splitSentenceAroundValue`) into an inline `.rule-value.rq-step__val` button + `.rq-range` (stepper buttons + real `<input type=range>`, same value/bounds/step); preview split into `__lede`/`__count`/`__guidance[data-band]` (new `bandForRatio`, a pure display categorisation of the already-returned `preview.ratio`, mirrors `preview.ts`'s own boundaries); removed the "Starts soft"/"Applies to all strategies" chips per card, added one shared line; include toggle relabelled "Included"/"Skip this one"; page sub-line copy updated. Found + fixed during rendered check: `.rq-pill` stretched full-width inside the new flex-column `li` and read as a primary `.rq-btn` — added a scoped `self-start`. Ran: `npm run verify` PASS (tsc/eslint/unit, 384 tests); `npx playwright test e2e/rules-guided-front-door.spec.ts` 3/3 PASS. Screenshots: `tmp/dev-screenshots/guided-rules-manual-{light,dark}.png` (390×844, throwaway user, deleted after) — matches #1.11 no-history state. `security-grep.mjs` clean. Flagged, not fixed (out of scope): `../new/RuleEditor.tsx` still claims "no range primitive exists" in its own header — stale since batch 1 shipped `.rq-range`.
- **2026-09-14 · PROCESS · First slice through dev-system v2 + cleanup.** Module 08 calibration restyle (`/rules/start` → frames 1.10/1.11): tier 1 by `npm run classify`; coder ran verify (tsc, eslint, 384 scoped unit tests), the one pinned E2E file (3/3), screenshots both themes, design-audit static pass; found and fixed a stretched `.rq-pill` reading as a primary. **Measured: 17 min wall clock, 1 agent, 186K tokens, 0 review agents** vs ~3 h / ~400K / 4 agents for the 2026-09-13 shell change. Tuning applied: design-build now tells the coder to pull one frame with `sed` instead of reading a whole screens file. Cleanup per owner: removed `explorations/`, the duplicate `retrospeq-design-system/.claude/skills/`, `~/Workspace/design-mockups/`, and the three interim artifacts; AGENTS.md / skills / qa now name `brand/docs/screens/` + `inventory.md` as the mockup authority, `instrument.html` as the superseded sketch.
- **2026-09-14 · DESIGN · Batch 7 brand kit — program complete.** `docs/landing.html` (brief-marketing: headline, the hook in a real frame, the loop, the Clear state, objections, pricing with `TODO(owner)` price/domain, honesty footer); `templates/email/` ×5 (table + inline styles, web-safe fallbacks, the only five emails the product sends); `templates/social/` post-square 1080², post-portrait 1080×1350 dark, post-wide 1600×900, `og` 1200×630 + rendered PNGs; `templates/pwa/` icon SVGs → 192/512/maskable/apple-touch PNGs, splash 1170×2532, `manifest.webmanifest`; `scripts/render-assets.mjs`; `docs/guidelines.html` (logo, colour, type, marks, components, motion, icons, voice & tone, copy patterns, surfaces, never). Fix during the look: `data-theme` must sit on `<html>` for a dark canvas. Looked at: landing, weekly email, og, portrait, guidelines. Owner review pending (artifact).
- **2026-09-14 · DESIGN · Batch 6 account + auth.** 9 states (`docs/screens/account.html`): sign in, sign up check-email + mailer-unavailable, two-factor + rate-limited, settings index, accounts list (connected / attention / syncing), account settings (disconnect vs delete), plan (fractions, data-derived upgrade, price `TODO(owner)`), security (2FA switch, recovery codes, sessions), privacy (export ready, telemetry, erasure cooling-off). New CSS: `auth`, `code`/`codes`, `settings`, `switch`, `account-card`, `usage`/`progress`/`upgrade-prompt`, `sessions`. Looked at: full page light. App mockup complete: 76 states.
- **2026-09-14 · DESIGN · Batches 4–5 review + performance.** 16 states (`docs/screens/review-performance.html`): the read ×5 (full, zero-prompt, week two, covers two weeks, no-trade week), decisions ×5 (graduation, relaxation equal pair, promotion, retirement equal pair, detection), deferred backlog, close, monthly trend, the one notification; Performance + not-enough-data. New CSS: `review`/`panel`/`findings`, `evidence`/`cost`/`decision-actions`/`choice`, `detection`, `backlog`, `stats`/`axis`. Looked at: full page light.
- **2026-09-14 · DESIGN · Batch 3 rulebook.** 20 states (`docs/screens/rulebook.html`): list, adherence, lifecycle controls + eligibility breakdown + retired, hard-cap swap, empty, guided front door, editor + preview bands + histogram threshold, tighten-only, discovery, inline edit; strategies list/gate/builder triggers/field picker/strategy screen/versions; fields list/editor/blocked states. New CSS: `rule`, `eligibility`, `discovery`/`catalogue`, `conditions`/`examples`, `field-group`/`field-list`, `cap-warning`, `radio-stack`, `field-states`, `dependents`, `versions`, `gate`, `steps`. Frame generator factored to `scripts/screengen.py`. Looked at: full page light.
- **2026-09-14 · OWNER · Round 1 rejected; complete `brand/`.** Owner reviewed the ten `/design-explore` directions and chose to keep the amber instrument system — no lock, no ADR. Exploration stays in `retrospeq-design-system/explorations/2026-09-14/` for the record. **DESIGN · Batch 2 trades:** 11 states (list, fills expanded, split/join equal pair, empty, pre-entry capture, ambient watch/breach, close-out ×5) in `docs/screens/trades.html`; new CSS for spec-named `trade`/`fills`/`not-a-decision`/`trim-reason`/`chips`/`closeout__trades`/`dir`/`day-label`. Looked at: full page light. Next: batch 3 rulebook.
- **2026-09-14 · DESIGN · Batch 1 home + onboarding.** 18 screen states (connect ×4, manual first trade, import, hook ×2, calibration ×2, home ×8 incl. degraded / grouping / milestone / field offer) as `brand/docs/screens/home-onboarding.html`, every element from the real stylesheet; frame chrome in `docs/screens/frame.css`. Spec-named markup given CSS for the first time: `chip`, `segmented`, `field`/`hint`, `verify`, `capability`, `rq-progress`, `sync`, `grouping-chip`, `milestone`, `offer`, `rule-sentence`/`rule-value`/`rq-range`/`preview`/`calibrate`, `alert--blocking`, `sr-only` — all state by text/weight/edge. `scripts/render.mjs` renders every frame light + dark (`docs/renders/`, gitignored). Copies re-synced. Looked at: full page light, 1.12 dark. Owner review pending (artifact). Next: batch 2 trades.
- **2026-09-14 · OWNER · Design direction stays `brand/`.** Sample mockups were produced by each original skill as written, brand rules allowed to break (`~/Workspace/design-mockups/`, README there lists what each broke; artifact 43618c96-f83a-469a-8d03-8c5af7acf403): ui-ux-pro-max → OLED navy/green + Inter/Playfair; bencium controlled → warm neutrals + terracotta + status colours; bencium innovative → ten typography-only directions A–J (unopened by Claude per its rule). Owner verdict: the existing amber instrument system is better; proceed with the master skills (`design-build`/`design-audit`) built from those sources, `brand/` as constitution. `design-explore` stays available but unused.
- **2026-09-14 · OWNER+PROCESS · UI/UX skills installed, master skills created.** Installed (Snyk "Top 8 Claude skills for UI/UX engineers", excl. #1/#4/#7 per owner) to `~/Workspace/design-skills/`: Vercel web-design-guidelines + react-best-practices + react-native-skills, ui-ux-pro-max, bencium controlled + innovative; pinned commits in its `SOURCES.md`; security-reviewed (no allowed-tools, Python is stdlib-only, one runtime WebFetch in WIG pinned locally). Trial of ui-ux-pro-max's `--design-system` for Retrospeq proposed navy + green accent + Inter/Playfair — contradicts the brand, so the master skill uses it for `ux`/`chart`/`react`/`--stack nextjs` lookups only. Master skills in `.claude/skills/`: `design-build` (procedure + `references/`: `retrospeq-rules.md` distilled authority with 20 hard rules and mockup→route map, `checklist.md` merged pre-delivery, `vercel-wig.md` pinned with sentence-case/no-status-colour overrides, `motion.md`, `external.md`), `design-audit` (static + rendered + keyboard pass, file:line output), `design-explore` (bencium innovative adapted: SVG allowed from Round 2, fixed product boundaries, owner-invoked only). react-native-skills installed but unused (web PWA). Coder/qa definitions and AGENTS.md point at the skills. Open question for owner: `retrospeq-design-system/.claude/skills/` holds an older vendored copy of ui-ux-pro-max + 6 sibling skills (commit e63c193) that now duplicate the install folder and add noise to the skill list — remove?
- **2026-09-14 · GATE · Dev system v2 verification · PASS with 5 known-flaky live tests.** Ran: `npm run check` 183 files / 2489 pass; `check:security` 18 files / 301 pass, no boundary violations; `e2e:changed` 10/10 (1.3 min, bypass on); `check:live` 77/81 files, 543/557 tests, **5 FAIL** — all in `confirm.live`, `adherence-repository.live`, `severity-lifecycle*.live`: 4 timeouts + 1 assertion on the unscoped `autoConfirmStaleTrades()` sweep. Environmental, not change-caused: none of those files changed in v2; same class as the tracked shared-DB backlog gap. Measured (read-only): 4 stale unconfirmed trades (all test users) and **837 orphaned test-pattern users of 850 total** — owner asked whether to purge them (destructive on the shared LuceEdge/Retrospeq project). Follow-up: `docs/infra-gaps.md` backlog entry updated with the numbers.
- **2026-09-14 · PROCESS · Development system v2** (owner-directed after the app-shell slice took ~3h across four gates for a markup-only change). Root causes: 2.2 MB ledger every agent was told to read in full; one fixed four-gate chain regardless of risk; mechanical checks re-run and narrated by LLMs; E2E suite structurally over the app's own sign-in rate limit; stale Windows-host notes. Changes: (1) `PROGRESS.md` capped at 200 lines, history archived to `docs/ledger/`, infra gaps to `docs/infra-gaps.md`; (2) risk tiers 0–3 from `scripts/classify-change.mjs`, gates per tier in `.claude/skills/verify/SKILL.md` — security-reviewer only for tier 3, qa only for tier ≥ 2 on non-negotiable surfaces or phase end, parallel when both; (3) `npm run check` / `check:security` / `check:live` / `verify` bundles + `.githooks/pre-commit` (ledger cap + eslint on staged); (4) `RETROSPEQ_E2E_RATE_LIMIT_BYPASS` fail-closed dev/test bypass (same shape as `devEntitlementToolsEnabled`) + Playwright `webServer` reuse + shared E2E user helpers; (5) all six agent definitions rewritten with an explicit input contract and ≤ 20-line ledger output; orchestrator `model: inherit`, others stay `sonnet` (cost is the owner's call); (6) skills `slice`, `verify`, `ledger`; (7) AGENTS.md slimmed to rules, narrative moved to `docs/process.md`; Windows-host notes removed.
- **2026-09-13 · OWNER · UI step 1 wrap-up.** App shell kept as interim (tester + security PASS; qa stopped by owner before verdict — not a pass). Sequencing changed: design-system build (owner process) → remaining features → screens. 109 throwaway test users cleaned from the shared dev project. Push access resolved (`lucedge` account).
- **2026-09-13 · OWNER · Visual authority.** `retrospeq-design-system/brand/` + `brand/docs/instrument.html` win over `modules/09-design-system.md` (indigo/Plex/shadcn superseded). Root cause of the "ugly UI": no phase ever scheduled a visual build; qa checked rules, never mockup fidelity; zero design-system marks used in `app/`.
- Older entries: `docs/ledger/decision-log-2026-08-19_to_2026-09-13.md` (156 entries) and per-gate slice reports in `docs/ledger/slice-reports-2026-09-11_to_2026-09-13.md`.
